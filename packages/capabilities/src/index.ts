/**
 * @harness-projects/capabilities —— 能力定义与注册契约
 *
 * Responsibility: 定义 Planning / Development / Delivery / Execution / Storage 的能力契约与注册机制，不含任何具体实现。
 * Allowed imports: @harness-projects/domain
 */

export const packageId = '@harness-projects/capabilities' as const
