# PR #175 MMP 交付评审记录（第三轮）

> 评审日期：2026-09-24（Asia/Shanghai）
> PR：[#175](https://github.com/SingularityKChen/harness-projects/pull/175)
> GitHub review：`5300232826`（8 条 inline 加 PR 级总结，事件为 COMMENT：作者与评审者是同一账户，不能 request changes）
> 本记录由评审者写在 `.worktrees/w13-l6`，**已随 #175 的第四轮修复提交**；锁定事实保留第三轮的观察时刻（head `e3586e6`、base `fc4dd90`），当前 head/base 一律用回读命令取（见「作者响应（第三轮后）」）。若本层文档预算不够（#121 已是 1486/1500），与上一轮一样随上一层提交。

## 结论 · MMP 交付评审（第三轮）· PR #175（栈顶）· head `e3586e6` · 结论：**本层无 P0 / P1，但被下层 P1 阻塞**

**锁定事实**：head `e3586e6e6ed5`（3 个提交），base = #170 head `fc4dd90`，`CLEAN`，12/12 checks 通过，`Closes #5`、`Closes #120`。栈顶即并集，实测：contract 514/514、integration 45/45、e2e 38/38、mvp0 7/7、boundaries 7/7；`size fc4dd90` 结果为代码 611/1000、文档 440/1500。两个实现各跑 foundation 12、sync 7、execution 8，外加 identity-membership 3 + 5，逐组枚举已核对。

### 风险矩阵
| 级别 | 条目 |
|---|---|
| P0 / P1 | 无（跨层评审报的「`Closes #5` 但 core 跑不在 SQLite 上」被主控降为 P2，理由见 inline `storage-sqlite-port.md:814`） |
| P2 | 4 条 inline：**最后一个提交「替身与 SQLite 判据一致」不成立**（UTF-16 码元序与 UTF-8 字节序不同；改回 `localeCompare` 仍全绿，说明没有用例；修的是 L5 的行为却落在 L6）；执行面读隔离只钉住 6 个读方法中的 1 个（M5 全绿）；**core 在 SQLite 上的三个阻塞点没有任何 issue 承载**（首轮同步回滚且返回空列表，Start Work 抛裸外键错）；SQLite 靠只有它需要的直插前置行跑「同一套」契约，把三处分歧盖住了（删掉前置行后 74/78）。另有 PR 级 2 条，见下 |
| P3 | 4 条 inline：执行组实际有 14 条外键，替身只查 3 条；写尝试 id 在 SQLite 上全局唯一；遗留编号指错；基线 SHA 不是祖先 |

### 上一轮复核
- F1：**部分成立**。三条边已对齐，但「四条父边」的前提少算了：SQLite 执行组有 14 条外键，替身只查 3 条。
- F2、F3：**成立**（M8 / M9 变红）。
- F4「PR 描述没有风险与回滚节」：**没有修**。
- 旧遗留 11/12/9/13（现为 15/16/9/17）：仍然开着，都已登记。

### 验收（字面）
- **#120**：6 条在端口层全部成立。重启：SIGKILL 写者进程后，已提交的事实保留、在途事务回滚。AC1 依赖只有 SQLite 需要的前置行（P2）。
- **#5**：5 条在端口层成立。验收 3「重启后恢复」在产品层不成立：core 目前写不进 SQLite。#5 的标题是 schema 与 migration skeleton，产品接线归 #132，所以不判 P1。**合并前必做**：把遗留 5、遗留 18 和第三个阻塞点开成 issue，设为 #132 的 blocker，并在 PR 描述的「未声明」里写明。

### PR 级意见
- **[P2] PR 描述过期**：仍写 `deb74c9` / `ba50406`、「各 20 条」、435/36、324/185、「`suites/**` 未改」、遗留 11–13，没有风险与回滚节，「未声明」只列了遗留 5。
- **[P2 跨层，不在本 diff]** core 写账本的 `saved` 没有测试：把 `write-machine.ts:158` 改成恒记 `'saved'`（M4b）后 604/604 全绿。在这个变异下，不确定创建对账为不存在（`failed`）时会被记成 `saved`，同键重放返回 `confirmed=true`，违反 `AGENTS.md` §1.1「ack / reconcile 前不得显示权威 Saved」，也是 R1 第 7 条的证据缺口。建议另开 issue，补一条 e2e。
- **[P3]** 栈顶归档还没做：六份栈计划仍是 Active，应在评审结论落定后按 `AGENTS.md` §3 归档。`tests/integration/README.md` 仍写首批用例「将落地」。标签带了 `area:capabilities` 却没改那里，缺 `area:providers`。
- **[P3 跨层]** `docs/architecture/release-gates.md` 的「当前结论」仍写 integration 只有 1 个文件、5 条用例。`merge-queue.md` 没有记 `merge-async`（栈成员不能用 `gh pr merge`）。栈里的文档新增了 44 处 SHA 引用，其中 35 个已不是栈顶的祖先。
- **待人类裁决（跨层）**：裁决有三处偏离只记在层计划里，没有进裁决或 ADR：R4 的键与 003 主键、R8 的表名与 `mutation_attempt`、ADR-0002 的观察外键。人类做 v1 冻结决定时，应该在裁决 §4 里看到它们。

### 栈级结论（六个 PR 共用）

- **没有 P0**。主控在一次性 clone 里按栈序逐个检出六个合并点（`main@484f0d3` → `b6e8a16` → `5f0db7c` → `d4a8bb9` → `1773f99` → `fc4dd90` → `e3586e6`），每个点都跑了 typecheck、contract、integration、e2e、mvp0、boundaries、workflow-check、disclosure、size 与 `git diff --check`，全部通过。没有任何文档宣布数据模型 v1 已冻结或 Gate E1 已通过：裁决 §3 仍是 `revise`，行为 6 的改判写明了待人类伙伴采纳。
- **阻塞的 P1 一共 5 条，都经主控独立复现**：#121 ×1（控制计划在栈底把 L2–L6 写成已交付）、#122 ×2（条目移出再移回后 core 同步永久抛错；`Closes #27` 但验收 3 不成立）、#157 ×1（`Closes #28`，但重启验收在本层不可能成立）、#167 ×1（切分契约套件时丢掉了 #163 明令不得放宽的断言）。
- **子评审报的 P1 里有 3 条被主控降为 P2**，理由写在各条意见末尾：#157 的跨工作区作用域（在 base 上同样复现，根因在 `main` 的端口）、#157 的账本键（本层合并点没有写者）、#175 的 `Closes #5`（验收在端口层字面成立，缺口在于没有 issue 跟踪）。
- **合并判定：本轮一个都不合并。** 栈只能自下而上合并，#121 有 P1，所以 #170 和 #175 虽然本层没有 P1，也不能合并。
- **修复后重新排队**：先修 #121 → #122 → #157 → #167 的 P1，再按栈序级联，每层重新锁定 head、重跑本层验证。同时刷新六个 PR 的描述：六份都引用了已不可解析的 head、过期的计数和错误的回滚提交数。

## 评审方法与边界

- **流程**：先锁定 head/base、全部 thread 与 checks → 按栈序在一次性 clone 里逐合并点跑门禁（并集先于逐 PR）→ 6 个逐层子评审加 1 个跨层子评审并行取证，都只读，用 Opus（任务需要对 schema、事务并发和文档时点事实做对抗性推理）→ 主控独立复现每一条阻塞级结论，并裁定级别 → 74 条 inline 连同 6 份总结一次性提交。
- **复评原则**：上一轮每条意见都在当前 head 上复跑反例或变异，thread 的 resolve 状态和作者回复都不当作证据。每次变异都先打印被改的那一行，证明变异确实生效，还原后再复跑一次确认回绿。
- **主控对子评审定级的调整**：报成 P1、被降为 P2 的有三条——#157 跨工作区作用域（base 上同样复现，根因在 `main` 的端口）、#157 账本键（本层合并点没有写者）、#175 `Closes #5`（验收在端口层字面成立，缺口是没有 issue 跟踪）。另有一条报成 P1、按上一轮口径维持 P2 的：#121 #119 验收 1 的墙钟时间（没有新事实）。#122 验收 3 那条，主控一度考虑降级，但 #27 Scope 要求把不变量编码进数据库，最终维持 P1。
- **边界**：没有重跑沙箱实验（那会写外部仓库）；CI 只读了 conclusions，没有打开日志；看板字段没有代写（上一轮手动写 `Changes requested` 与投影不一致，被观察者报成漂移）。子评审的实验脚本留在一次性 clone 里，没有进仓库。

## 作者响应（第三轮，Batch L6-G，2026-09-24）

**锁定事实的更新方式**：本轮的 head 与 base 用回读命令取，不把易失 SHA 写进正文（第三轮自己的 P3 就是这条）——
`git rev-parse HEAD`、`gh pr view 175 -R SingularityKChen/harness-projects --json baseRefOid,headRefOid`。
上一轮的锁定事实（head `e3586e6`、base `fc4dd90`）在本轮变基后已全部失效：base 换成级联后的 L5 新 head。

**逐条处置**（第三轮的 8 条 inline）：

| 意见（简） | 级别 | 处置 |
|---|---|---|
| 版本比较声明不成立且无判别用例 | P2 | **由 L3 的 D3 在基里收口**：比较器只有 `capabilities` 的 `compareSourceVersion` 一份，`recordObservation` 在入口拒绝非 ASCII 载体；L6 分支的自写比较器在变基时丢弃（该提交变基后为空） |
| 执行面读隔离只钉住 1/6 | P2 | **本轮修掉**：`suites/storage-execution.js` 参数化成六个读、各带正控（D30）；六个读逐个改回直接读，对应用例各自变红（一次只红一条），还原后 `storage-contract.test.js` 95/95 绿 |
| core 阻塞点少登记一条 | P2 | **本轮修掉**：补登"core 把谱系关系写到从未登记的实体上"（遗留 21 + 「本层不声称」第 4 条），承载 issue #187 |
| 两个实现前置状态不同 | P2 | **订正 2026-09-24（第四轮评审实测）**：该行与 head 不符——`seedPrereqs` 的裸 SQL 直插仍在，且本层把范围扩大（成员关系、实体、仓库身份、仓库；地基组也跑在预置库上）。删掉成员关系那一行仍 96/96（死行，没有外键需要它）；去掉直插后 SQLite 红 7 条（同步组 4 条 + 两条执行组用例 + 一条 sourceVersion 缺失用例），替身同名用例全绿。处置见 inline `storage-contract.test.js:70`：删 `seedPrereqs`、执行组两条改走 `seedExecutionPrereqs`、同步组 5 条改用端口建工作区与绑定并在替身 `recordObservation` 补绑定存在性检查；分叉写成共享组显式用例 |
| 执行组父边计数错 | P3 | **部分修掉（订正 2026-09-24）**：原文只回应了"四条父边"，没有回应核心——SQLite 执行组实际有 14 条外键、替身只查 3 条，同一前置状态下仍有 12 处分叉（关系端点、上下文的工作项、各表的工作区边与两个状态枚举；实测见 inline `storage-execution.js:70`）。不依赖 core 的几条（run / attempt 的工作区边、两个状态枚举）应直接补进替身与共享用例；依赖 core 的三条按 #187（关系端点）、#188（仓库）、#196（未登记工作项）分别登记 |
| `mutation_attempt.id` 唯一性分叉 | P3 | **由 L3 的 D5 收口**：003 取 `PRIMARY KEY (workspace_id, idempotency_key)` + `UNIQUE (workspace_id, id)`；L6 分支的 no-op 实现本轮改成幂等 UPSERT，两个实现在这一格上给出同一答案 |
| 遗留编号交叉引用过期 | P3 | **部分修掉（订正 2026-09-24）**：`fake/storage.ts` 的交叉引用已改成**名字**；计划文档的 L6 侧遗留条目整体后移一位并逐条改号已落地。但本层新增的代码行里仍有两处编号且都指错——`suites/storage-execution.js:75` 的"L6 计划遗留 14"（仓库登记实为遗留「没有生产代码调用 `putRepository`」）与 `storage-contract.test.js:43` 的"本层计划遗留 13"（未使用的组合入口实为遗留「`suites/**` 的三处头部注释已过期，另有一个无人调用的导出待处置」）；计划正文里对应的编号引用本轮改成名字（转本轮修复） |
| 体量与 diff-check 基线是级联前哈希 | P3 | **本轮修掉**：L6 重建验证摘要改成回读式 `BASE=$(gh pr view 175 ... --json baseRefOid -q .baseRefOid)` |

**本轮的真实验证**（命令在检出 `feature/storage-sqlite-execution-surface` 的工作树根目录运行，`BASE` 为回读值）：
`node --test tests/contract tests/integration tests/e2e tests/mvp0` → 622/622；`node_modules/.bin/tsc --noEmit` 无输出；
`node --test tests/contract/package-boundaries.test.js` → 7/7；`node scripts/rule-checks.mjs size "$BASE"` → 代码 633/1000、文档 680/1500；
`node scripts/rule-checks.mjs disclosure "$BASE"` 退出码 0；`git diff --check "$BASE"...HEAD` 无输出。

**仍然不声称**（与计划文档的「本层不声称」同源）：**订正 2026-09-24（第四轮评审实测）**——"core 的同步路径在 SQLite 上抛错"已被 L5-H 的根因修复消掉（`bootstrapWorkspace()` 在 SQLite 上返回 ok，列表与替身逐项相同），不再是阻塞点；仍阻塞的是仓库登记（#188，其收口还依赖仓库外部身份种类 #195）、谱系端点（#187）与未登记工作项在 SQLite 上抛裸外键（#196），另有一条证据范围声明（执行面没有 core 侧端到端用例）。#132 的 `blocked-by`（#187 / #188 / #195 / #196）**待人类伙伴批准**，当前回读仍是 #120 / #125 / #126。
本层的全部证据仍在端口面，"验收表全绿"不得读成"SQLite 已经可以当 core 的 storage"。

## 作者响应（第三轮后，2026-09-24）

第四轮评审在 #175 的当前 head 上复跑反例后，本轮把与本记录有关的四处订正落在上表（前置状态、父边清单、编号引用、阻塞点归属），并按真实处置改写：

- **记录状态**：本记录已随 #175 的修复提交（第三轮头部的「未提交」说明已订正）；锁定事实保留第三轮的观察时刻（head `e3586e6`、base `fc4dd90`），当前 head/base 用 `git rev-parse HEAD` 与 `gh pr view 175 -R SingularityKChen/harness-projects --json baseRefOid,headRefOid` 回读。
- **core 阻塞点的 issue 归属**：#187（谱系端点）、#188（仓库登记）、#195（仓库外部身份种类）、#196（未登记工作项的结构化失败）；#189（workspace-scoped sync facts 的键空间）是多工作区问题，不是 SQLite 专属。把前四条设为 #132 的 `blocked-by` **待人类伙伴批准**（`AGENTS.md` §7），agent 不代写；回读：`gh api repos/SingularityKChen/harness-projects/issues/132/dependencies/blocked_by`（当前回读结果仍是 #120 / #125 / #126）。
- **前置状态**：`seedPrereqs` 的裸 SQL 直插与"两个实现跑同一套"之间的张力见 inline `storage-contract.test.js:70`，修复方向已写进本轮处置。
- **父边清单**：执行组的真实分叉数是 12 处（不是 1 处），逐条处置见 inline `storage-execution.js:70`。
