# PR #122 MMP 交付评审记录（第三轮）

> 评审日期：2026-09-24（Asia/Shanghai）
> PR：[#122](https://github.com/SingularityKChen/harness-projects/pull/122)
> GitHub review：`5300230688`（20 条 inline 加 PR 级总结，事件为 COMMENT：作者与评审者是同一账户，不能 request changes）
> 本记录由评审者写在 `.worktrees/w8-s2`，已随 L2（PR #122 的 2026-09-24 响应轮）提交入库。以下「锁定事实」与「结论」都是**评审时点**的快照；当前 head、checks 与处置以文末「作者响应」的回读命令为准。

## 结论 · MMP 交付评审（第三轮）· PR #122 · head `5f0db7c` · 结论：**Blocked（2 × P1；下层 #121 另有 1 × P1）**（评审时点结论，处置见文末「作者响应」）

**锁定事实**：head `5f0db7cd956c`（7 个提交），base = #121 head `b6e8a16`，`CLEAN`，12/12 checks 通过，`Closes #27`。本合并点实测：contract 464/464、integration 18/18、e2e 38/38、mvp0 7/7、boundaries 7/7；`size b6e8a16` 结果为代码 **999/1000**、文档 1040/1500（PR 描述写 1016）。

### 风险矩阵
| 级别 | 条目 |
|---|---|
| P0 | 无 |
| **P1** | ① **条目移出 project 再移回后，core 同步永久抛错**（inline `storage.ts:147`）。本 PR 新增了「投影的实体必须存在」检查，而替身原有的「移除陈旧投影时删实体、留身份」会留下悬空身份，两者叠加后出错。主控在 base / 本层 / 栈顶三个 head 上跑同一段 `composeCore` 脚本：base 上正常，另外两个都抛 `projection entity does not exist`。宿主当前用的就是内存替身，这条路径今天可达。② **`Closes #27`，但验收 3「恰好一个 primary」仍然只做到「至多一个」**（inline `002:66`）。#27 Scope 要求把不变量编码在数据库里，而延迟外键实测可以声明式表达，推翻了注释里的前提；计划承诺的生命周期断言没有交付，分工也没有人类批准。 |
| P2 | 9 条 inline：同 id 换 `implementationKey` 的拒绝与 core 的 `implementationKey: domain` 冲突；解除挂载或切换 planning 源后旧来源成为孤儿；连接锚点没有账号自然键；三处用例没有判别力（M1、M5–M7 全绿）；替身 `putExternalIdentity` 与 002 在 8 格上分歧；本层文档写入上层事实；随本层提交的评审记录没有更新；**连接跨工作区共享后，以 `bindingId` 为键的工作区级事实没有被审计**（主控复现：freshness 被另一个工作区覆盖。base 上同样发生，所以判 P2，根因在端口） |
| P3 | 9 条 inline：`002:105` 仍写「不可达」；凭据列审计与守卫正则；约束出处；`entity.kind` 是自由文本；「用户裁定」缺批准者、时间与出处；端口注释「不抛错」与新增的拒绝规则矛盾；文件所有权；套件卫生；评审响应计划不在 `docs/README` 索引里 |

### 上一轮复核
- P1-1「身份键挂在单工作区绑定上」：**成立**。同一锚点、同一对象进两个工作区，得到 1 条身份、1 个实体、2 条成员关系、2 行投影；经 core 组合也成立。残留：账号自然键问题，转为 P2。
- P1-2「验收 1/3/5」：**部分成立**。验收 1 成立（大小写变体、`enabled` 取 2 / `'1'` / NULL / `X'01'` 都被拒）；验收 3 不成立，是本轮 P1 ②；验收 5 部分成立。
- 5 格分歧：**部分成立**（原来的 5 格已对齐，新发现 8 格）。计划与 PR 描述过期：**部分成立**。约束出处：**部分成立**。「不可达」：**部分成立**。`#109`：**没有修**。
- `domain` 自由文本、哨兵正则、`enabled = 1`、project 分量与死约束、`listMemberships` 顺序：**成立**，都已用变异实验确认会变红。

### #27 验收（字面）
| # | 判定 |
|---|---|
| 1 第二个启用的 planning 绑定由库拒绝 | 满足 |
| 2 change request 成员关系不产生工作项 | 满足（在系统层，由 base 上已有的 core `entityKindFor` 与 e2e 保证） |
| 3 恰好一个 primary | **不满足** → P1 ② |
| 4 表与约束可追溯 | 部分 |
| 5 无凭据列 + 显式审查 | 部分 |
| 6 迁移从空库可重复 | 满足 |

### PR 级意见
- **[P2] PR 描述过期**：仍写 head `4d86a96`、文档 1016、「3 个提交」和 `#109`。
- **[P2] 提交序列**：保留了已被整体改写的上一轮设计，`fc867f3` 属于 fixup 性质（`AGENTS.md` §6）。
- **[P3] 代码 999/1000 压线**：新增代码里有 52 行超过 160 字符，另有用 `;` 拼接的行。没有违反规则，但修 P1 时没有余量，需要同批压缩。
- **需要人类伙伴确认**：ADR-0006 的「用户裁定」在仓库内没有可核对的出处（inline `stack-review-response.md:278`）。

### 栈级结论（六个 PR 共用）

- **没有 P0**。主控在一次性 clone 里按栈序逐个检出六个合并点（`main@484f0d3` → `b6e8a16` → `5f0db7c` → `d4a8bb9` → `1773f99` → `fc4dd90` → `e3586e6`），每个点都跑了 typecheck、contract、integration、e2e、mvp0、boundaries、workflow-check、disclosure、size 与 `git diff --check`，全部通过。没有任何文档宣布数据模型 v1 已冻结或 Gate E1 已通过：裁决 §3 仍是 `revise`，行为 6 的改判写明了待人类伙伴采纳。
- **阻塞的 P1 一共 5 条，都经主控独立复现**：#121 ×1（控制计划在栈底把 L2–L6 写成已交付）、#122 ×2（条目移出再移回后 core 同步永久抛错；`Closes #27` 但验收 3 不成立）、#157 ×1（`Closes #28`，但重启验收在本层不可能成立）、#167 ×1（切分契约套件时丢掉了 #163 明令不得放宽的断言）。
- **子评审报的 P1 里有 3 条被主控降为 P2**，理由写在各条意见末尾：#157 的跨工作区作用域（在 base 上同样复现，根因在 `main` 的端口）、#157 的账本键（本层合并点没有写者）、#175 的 `Closes #5`（验收在端口层字面成立，缺口在于没有 issue 跟踪）。
- **合并判定：本轮一个都不合并。** 栈只能自下而上合并，#121 有 P1，所以 #170 和 #175 虽然本层没有 P1，也不能合并。
- **修复后重新排队**：先修 #121 → #122 → #157 → #167 的 P1，再按栈序级联，每层重新锁定 head、重跑本层验证。同时刷新六个 PR 的描述：六份都引用了已不可解析的 head、过期的计数和错误的回滚提交数。

## 评审方法与边界

- **流程**：先锁定 head/base、全部 thread 与 checks → 按栈序在一次性 clone 里逐合并点跑门禁（并集先于逐 PR）→ 6 个逐层子评审加 1 个跨层子评审并行取证，都只读，用 Opus（任务需要对 schema、事务并发和文档时点事实做对抗性推理）→ 主控独立复现每一条阻塞级结论，并裁定级别 → 74 条 inline 连同 6 份总结一次性提交。
- **复评原则**：上一轮每条意见都在当前 head 上复跑反例或变异，thread 的 resolve 状态和作者回复都不当作证据。每次变异都先打印被改的那一行，证明变异确实生效，还原后再复跑一次确认回绿。
- **主控对子评审定级的调整**：报成 P1、被降为 P2 的有三条——#157 跨工作区作用域（base 上同样复现，根因在 `main` 的端口）、#157 账本键（本层合并点没有写者）、#175 `Closes #5`（验收在端口层字面成立，缺口是没有 issue 跟踪）。另有一条报成 P1、按上一轮口径维持 P2 的：#121 #119 验收 1 的墙钟时间（没有新事实）。#122 验收 3 那条，主控一度考虑降级，但 #27 Scope 要求把不变量编码进数据库，最终维持 P1。
- **边界**：没有重跑沙箱实验（那会写外部仓库）；CI 只读了 conclusions，没有打开日志；看板字段没有代写（上一轮手动写 `Changes requested` 与投影不一致，被观察者报成漂移）。子评审的实验脚本留在一次性 clone 里，没有进仓库。

## 作者响应（第四轮前，2026-09-24）

第三轮的 20 条 inline 已逐条处置，理由与证据见 `docs/exec-plan/active/2026-09-24-review-root-cause-convergence.md` 的逐条处置表。两个 P1 的落点：① 移出再移回：替身不再删除仍有身份指向的实体（`suites/storage.js` 的「投影被收敛移除后实体与身份保留，条目可以重新加入」钉住，第五轮评审的变异 F27 使其变红）；② 「恰好一个 primary」的生命周期断言改在 `tests/e2e/chain-bootstrap.test.js` 上按 provider 条目逐项断言，绕过会跳过零 primary 的查询过滤。

回读（在检出 `feature/storage-identity-membership-tables` 的工作树根目录运行）：
- `node --test tests/contract tests/integration tests/e2e tests/mvp0`（期望 fail 0）
- `BASE=$(gh pr view 122 -R SingularityKChen/harness-projects --json baseRefOid -q .baseRefOid)`，再 `node scripts/rule-checks.mjs size "$BASE"`（期望代码 ≤1000、文档 ≤1500，exit 0）
- `gh pr view 122 -R SingularityKChen/harness-projects --json headRefOid,baseRefName`
