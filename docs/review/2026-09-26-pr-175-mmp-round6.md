# PR #175 MMP 交付评审记录（第六轮 · 栈顶收口）

> 状态：无 P0 / P1 代码问题；两条非代码 P1 按「合并前必修」由评审者就地修复，修复后依次合并 #170、#175
> 评审日期：2026-09-26（Asia/Shanghai）
> 评审账号：Singularity-AI-Bot（`gh-review`），GitHub review `5324991587`（#175，10 条 inline）与 `5324991438`（#170，9 条 inline）；修复与提交用开发账号 SingularityKChen
> 锁定事实是评审时点的快照；当前 head 以 `gh pr view 175 -R SingularityKChen/harness-projects --json headRefOid,baseRefOid,closingIssuesReferences` 回读

## 锁定事实（评审时点）

- #175：head `b6d5283`（4 个提交），base = #170 head `e6bc5a4`，`CLEAN`，16/16 checks 通过，31 个 thread 已全部 resolve，`closingIssuesReferences = [28, 120]`，`Refs #5`。
- 栈底 #121–#167 已于 2026-09-25 以 rebase merge 合入；`main@53aadd5` 上有第五轮的批次记录 `docs/review/2026-09-26-mmp-round5-batch-review.md`。
- 逐合并点门禁（一次性 clone）：
  - #170：contract 531/531、integration 62/62、e2e 39/39、mvp0 7/7；代码 971/1000、文档 668/1500。
  - #175：contract 561/561、integration 69/69、e2e 39/39、mvp0 7/7；代码 772/1000、文档 589/1500。
  - 两点的 boundaries、workflow-check、disclosure、`git diff --check` 均通过。

## 评审方式

- 先跑门禁，主控独立核对第五轮 P1 的补回位置，再派 #170、#175 两个逐层子评审（Opus）与一个流程审计（Sonnet）。子评审只读，只写 `comments.json`。
- 防丢内容：各层改动与第四轮 head（`ae39655` / `693aba4`）逐文件对照。
- 阻塞级结论由主控独立复现；非代码 P1 的定级依据写进 inline。

## 结论

- **第五轮两条 P1 已修复**：
  - SQLite 装配了执行组（13 条）与身份同步组（5 条），删掉任一装配，装配守卫变红。
  - `replacePlanningProjections` 走 `atomic`，改回 `mutate` 会变红；base 上被删的重启用例放到 head 上跑，4/4 通过。
  - `putExecutionContext` / `putRelation` 改走 `atomic` 后各有原子性用例钉住。
- **非代码 P1 ×2，定为「合并前必修」**：
  - `Closes #28` 与 `main` 上收敛计划的 Decision Log「#28 与 #5 的关闭归属待人类伙伴决定」冲突，理由之一是验收 3「库不接受确定性行直接进确认表」尚未在任何层成立。主控复现：库和端口都接受 `deterministic + confirmed` 直接写进确认表；验收 1 要求「进程重启、逐字节」，现有用例是同一进程内关句柄再重开。
  - 层计划在级联时再次丢失 L6 的记录：第四轮 1292 行、当前 1221 行，标题和范围只写到 L5，L6-A、D30、Surprises 23–28 等缺失。
- **产品闭环**：`composeCore` + SQLite 文件库上，bootstrap、列表、详情、重启与离线降级都与内存替身一致；Start Work 在 SQLite 上仍抛裸 `FOREIGN KEY`（仓库 #188、关系端点 #187、工作项 #196），外部副作用为 0。没有任何宿主 import `storage-sqlite`，用户仍然看不到变化。#5 保持 `Refs` 是对的。
- **#120 六条验收字面成立**：
  - SQLite 跑全部契约组与分叉格；
  - 重启用例覆盖身份、关系与执行上下文；
  - 失败事务不留行，对重开的库断言；
  - 端口写过的库重跑迁移，`applied=[]` 且行不变；
  - 文件库上观察去重、写入幂等；
  - boundaries 通过。

## 就地修复

| 意见 | 修复 | 证据 |
|---|---|---|
| 非代码 P1：`Closes #28` | PR 描述与索引改为 `Refs #28`；在 #28 登记证据与待人类决定项 | 合并前回读 `closingIssuesReferences` 只剩 `[120]` |
| 非代码 P1：层计划丢失 L6 记录 | 以第四轮 L5 的计划为 base，把第四轮 L6 的改动三方合并到当前计划，逐冲突裁决；保留第五、六轮的订正；标题、状态、范围覆盖 L4–L6 | `plan-facts-consistency` 与 `e1-evidence-consistency` 通过 |
| P2：`execution_run` 的同工作区复合外键在替身上没对齐；关系 state / source 枚举没有用例 | 替身按 `(workspaceId, contextId)` 复合判定；共享用例补跨工作区运行与两条非法枚举，并断言不留行 | 三个变异（复合判定改回只比 id、删 `RELATION_STATES` 检查、删 `RELATION_SOURCES` 检查）各红 1 条，还原后 121/121 |
| P2：#188 先于 #187 修复时，重试会报 `saved / confirmed` 但运行从未启动 | 写进层计划的遗留，并在 #187 登记 | — |
| P2：栈顶没有收口跨层计划 | 控制计划、收敛计划、响应计划与 L4–L6 层计划一起移入 `completed/`，状态、收尾项、索引与全仓引用同步；`plan-facts-consistency` 的下限断言改为允许控制计划位于 `completed/` | 全仓 `docs/exec-plan/active/<四份计划>` 引用 0 处 |
| P3：分叉守卫数的是循环次数；SQLite 一侧接受任何原因的拒绝 | 计数挪进 `register` 的包装；SQLite 一侧断言拒绝原因是 `FOREIGN KEY constraint failed` | 注册改成 `if (false)` 时守卫红；关系一格改为「不悬空、只是类别非法」时红 |
| P3：注释过期 | `ADDED_CASE_COUNT` 按实际拆成地基 6 + 执行 10；装配注释改为 L6 现状，并写明三格分叉的来源 | — |
| P3：第五轮记录的锁定事实 | 头注改为快照说明，并指向本记录 | — |
| PR 级：标签、`release-gates.md` 的「当前结论」、`tests/integration/README.md`、提交正文 | 标签按 diff 订正；两处「当前结论」更新到本 head 并标注为快照；第一批用例逐项标注落地文件（第 5 项随 #160）；提交重整为可独立回退的交付物并补正文 | — |

## 合并顺序与回读

1. #170 先合（merge-async），#175 的 base 自动改为 `main`；再执行 `git rebase --onto origin/main <#170 合并前的 head>`，精确 lease 推送，回读 checks 后合并 #175。
2. 合并后回读：#164、#120 为 CLOSED；#28、#5 仍 OPEN，并带本轮登记的证据与待决项；`main` 上四份跨层计划位于 `completed/`。

## 转交人类伙伴

- #28、#5 的关闭归属，以及验收 1（进程重启 / 逐字节）、验收 3（库层拒绝确定性行直接进确认表）是否收窄。
- #132 的 blocked-by（#187 / #188 / #195 / #196）需要人类批准（`AGENTS.md` §7）。
- 裁决 R4 / R8 的偏离与 v1 是否冻结。
