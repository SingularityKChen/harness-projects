# PR #122 MMP 交付评审记录（后续轮）

> 状态（评审时点）：Blocked（2 × P1；所依赖的 #121 另有 1 × P1；本轮不合并）；处置见文末「作者响应（第三轮，2026-09-24）」
> 评审日期：2026-09-24（Asia/Shanghai）
> PR：[#122](https://github.com/SingularityKChen/harness-projects/pull/122)，声明 Closes #27，Refs #5 #28 #119
> 并集实测与栈底结论：`docs/review/2026-09-24-pr-121-mvp-review.md`（写在 `.worktrees/w7-e1-5`）
> 本记录由评审者写在 `.worktrees/w8-s2`，已随 L2 提交入库；以下「锁定事实」是评审时点快照，响应后的 head 与 checks 以文末回读命令为准。
> GitHub review：`5298680424`（10 条 inline + PR 级总结）

## 锁定事实

- head `7138a5d30ecb`（4 个提交），base `test/e1-uncertain-create@74086b2ee4df`（= #121 评审 head）；checks 全绿。
- 评审 head 上的实测（在检出 `feature/storage-identity-membership-tables` 的 `.worktrees/w8-s2` 根目录运行）：`node --test tests/integration` → 15 / 15；`node --test tests/contract/storage-contract.test.js` → 20 / 20；`node --test tests/contract/package-boundaries.test.js` → 7 / 7；`node scripts/rule-checks.mjs size origin/test/e1-uncertain-create` → 代码 592、文档 472。
- 并集（#121 + #122 变基到 `main@484f0d3`）：`pnpm verify` 508 / 508 + 7 / 7，并集层无 P0。

## P1（阻塞）与独立复现

评审者在内存 SQLite 上依次执行本 PR head 的 `001_init.sql` 与 `002_identity_membership.sql`（`git show` 取文件，只读）：

```text
ACCEPTED  AC1 第一条 enabled+default planning 绑定
ACCEPTED  AC1 第二条 enabled、非默认 planning 绑定（同一工作区）
ACCEPTED  AC1 第三条 enabled+default，domain 写成 Planning
    enabled planning-like bindings in A: 3
ACCEPTED  AC3 两条身份都为 alias（零个 primary）
    primaries for E1: 0
REJECTED  B1 同一绑定 id 挂到第二个工作区 :: UNIQUE constraint failed: provider_binding.id
ACCEPTED  B1 A 下登记 I_shared
ACCEPTED  B1 B 下再登记同一个 I_shared（只能挂 B 自己的绑定）
    identity rows for I_shared: 2 | distinct entities: 2
```

1. **身份键挂在单工作区绑定上**（`002:42` 的 `UNIQUE (binding_id, external_kind, external_id)`、`:36`、`:17`）：同一对象进入第二个工作区必然得到第二条身份与第二个实体，Gate E1 行为 1 与 ADR-0001 的 Consequences 在 002 里表达不了（不变量 6 的"反复重新识别"）。#27 Scope 里的 connector accounts 表没有建，也没有登记推迟。
2. **`Closes #27`，但验收 1、3 字面不成立，验收 5 未交付**：只拒绝"启用且默认"的第二个绑定（本 PR 用例 `:138` 反向断言），`domain` 自由文本可绕过；只保证至多一个 primary；没有显式的凭据 schema 审查。合并会在没有人类批准的情况下接受这些收窄。

## #27 验收

| # | 判定 |
|---|---|
| 1 第二个启用的 planning 绑定被库拒绝 | 不满足（字面） |
| 2 change request 成员关系不再产生工作项 | 部分：库与端口接受禁态；系统层靠 core `entityKindFor` + e2e |
| 3 恰好一个 primary | 不满足（字面） |
| 4 表与约束可追溯 | 部分 |
| 5 无凭据列 + 显式审查 | 部分 |
| 6 迁移从空库可重复 | 满足 |

## P2 / P3

逐条见 GitHub review `5298680424`："第二个工作项在 schema 上不可达"不成立、"行为 2"用例同义反复；替身与 SQLite 在 5 个输入上分歧、后者胜的孤儿语义未定义；R1 第二条唯一约束的 project 分量没钉住、第一条是死约束；计划与 PR 描述仍描述已删设计（验收 #8、两个 #14、十张表映射、D11）；`domain` 自由文本；哨兵正则不区分约束类型；`enabled = 1` 未钉住；部分约束无出处；`listMemberships` 顺序未约定；PR 描述里的 `#109`。

## 重新进入排队的条件

- 身份键与连接实体的作用域由人类裁定（跨工作区的 connector account / 全局 binding，或"一个库只放一个工作区"），写进 ADR。
- 实现改到 #27 的字面验收，或由人类改写验收并记入 Decision Log；否则改为 `Refs #27`。
- 变基到修复后的 #121，按栈序级联 #157 → #175 并各自复跑验证。

## 看板写入记录（`AGENTS.md` §7）

- 2026-09-24T01:55:20Z，actor = agent（按人类伙伴在会话中的明确指令代写），目标 project 10（`PVT_kwHOAY1ahM4BjzAQ`）条目 `PVTI_lAHOAY1ahM4BjzAQzg7hyvs`（issue #27）字段 `Engineering`（`PVTSSF_lAHOAY1ahM4BjzAQzhiqjsA`）：`PR open` → `Changes requested`（`0d1cbcb3`），幂等键 `clientMutationId = engineering-changes-requested-2026-09-24:PVTI_lAHOAY1ahM4BjzAQzg7hyvs`；回读 `Engineering=Changes requested`、`Status=In Review` 未变。
- **这是一次与投影不一致的手动写入**：#122 的 `reviewDecision` 为空（评审者与作者是同一账户，GitHub 不允许 request changes，本轮 review 只能是 COMMENT），`stateForSnapshot` 的投影是 `PR open`。写入后只读观察者 `check-engineering-drift-live.mjs --json` 立即报出 `expected: 'PR open', actual: 'Changes requested'`；#122 的下一次生命周期事件会让写入口 reconcile 回 `PR open`。让它持久成立的途径是由作者以外的评审者提交 Request changes。

## 作者响应（第三轮，2026-09-24）

本记录随本层进入 `main`，因此「Blocked / 未提交」是**评审时点**的结论。第三轮的 14 / 20 条 inline 已逐条处置，理由见 `docs/exec-plan/completed/2026-09-24-review-root-cause-convergence.md` 的逐条处置表；本记录的「锁定事实」不再写死 head 与计数，改为回读命令。回读：`gh pr view 122 -R SingularityKChen/harness-projects --json headRefOid,baseRefName`；`node --test tests/contract tests/integration tests/e2e tests/mvp0`。
