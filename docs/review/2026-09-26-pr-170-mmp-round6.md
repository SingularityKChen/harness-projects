# PR #170 MMP 交付评审记录（第六轮）

> 状态：无 P0 / P1；评审者就地修复 P2 / P3 后合并
> 评审日期：2026-09-26（Asia/Shanghai）
> 评审账号：Singularity-AI-Bot（`gh-review`），GitHub review `5324991438`（9 条 inline 与 PR 级总结）；修复与提交用开发账号 SingularityKChen
> 锁定事实是评审时点的快照；当前 head 以 `gh pr view 170 -R SingularityKChen/harness-projects --json headRefOid,baseRefOid` 回读

## 锁定事实（评审时点）

- head `e6bc5a4`（4 个提交），base `main@53aadd5`（#121–#167 已于 2026-09-25 合并），`CLEAN`，16/16 checks 通过，41 个 thread 全部已 resolve，`Refs #164`。
- 本合并点实测（一次性 clone）：contract 531/531、integration 62/62、e2e 39/39、mvp0 7/7、boundaries 7/7；`size` 代码 971/1000、文档 668/1500；disclosure、workflow-check、`git diff --check` 均干净。

## 评审方式

- 先跑逐合并点门禁（#170、#175 两点）；主控独立核对第五轮三条 P1 的补回位置，再派两个逐层子评审（Opus）与一个流程审计（Sonnet）。子评审只读，只写 `comments.json`。
- 防丢内容：本层改动与第四轮 head `ae39655` 逐文件对照，每一处缺失都判定是有意删除还是丢失。
- 判别力：补回的用例逐条做变异。每次先打印被改的行，确认变异生效，还原后回绿。

## 结论

- **第五轮 P1「改写历史丢失判别性用例」已修复。** 指定的 7 个变异都会变红：`putMembership` / `replacePlanningProjections` 改走 `mutate`、去掉 `object_kind`、删掉 SQLite 身份同步组装配、去掉结算令牌、关掉旧库自检、自检失败不关句柄。唯一存活的「只删入口检查」是等价变异，因为执行时的检查给出同一个拒绝。
- **产品闭环**：`composeCore` + SQLite 文件库上，bootstrap、列表、详情、重启与离线降级都和内存替身逐项一致，这是本层让它成立的；base 上会抛 `not implemented in L4: getSyncCursor`。但没有任何宿主 import `storage-sqlite`，用户仍然看不到变化；Start Work 要到 L6。
- **#164 五条验收字面成立**：两个实现各跑 8 条同步组用例；文件库上 N 次投递等于一次；`v1 → v3 → v2` 得 `[T,T,F]`，空串在入口就被拒绝；成员关系走 upsert、后到者胜；缺父行被拒且不留行；只依赖 domain 与 capabilities。作者以 #189（游标缺工作区维度）为由保留 `Refs`，但 #189 是 `main` 上端口的既有缺口，不在 #164 的验收里。**改为 `Closes #164`。**

## 就地修复（P2 / P3）

| 意见 | 修复 | 证据 |
|---|---|---|
| P2 共享同步用例绕过装配台账 | 循环改为组函数 `sharedSyncSuite`，经 `assemble` 装配；替身与 SQLite（同步组）的期望各补 `sharedSync` | 删掉 SQLite 的 `sharedSync` 装配后装配守卫红（80/81）；再叠加 `putMembership` 改 `mutate`：修复前四层 629/629 全绿，修复后 628/629；还原后 639/639 |
| P2 #164 没有关闭 | PR 描述与提交正文改为 `Closes #164` | 合并后回读 `gh issue view 164 --json state` |
| P3 装配处注释与代码矛盾 | 文件头改为三条 SQLite 专属用例；共享用例说明改为「经 `assemble` 装配、唯一的直连是注入触发器」 | — |
| P3 层计划 | L5-C 的验证块改按 #170 回读 BASE；L5-D 的写死期望改为 `fail 0`；验收 35 标注快照；删掉重复的旧版 L4-F 回滚；行数改为 `wc -l` 回读；回退说明改为按新的提交布局写；Change Note 引用改为验收第 41 项 | `git diff --check` 干净 |
| P3 收敛计划的根因归属 | 装配台账是 L5 在 `ae39655` 加上、又由 L5 自己在 `d84bc16` 删掉的，L4 的任何版本都没有 | `git show <rev>:tests/contract/storage-contract.test.js \| grep -c EXPECTED_ASSEMBLY`：`89d1a9e`、`de95f94`、`main` 为 0，`ae39655` 为 3 |
| P3 第五轮记录 | 头注改为「随第六轮修复提交、锁定事实是快照」；`a3c46b8` 标注为远端不可解析 | — |
| PR 级：标签、回滚提交数、提交 ① 的正文 | 标签按 diff 订正；提交重整为「实现 + 用例」「计划与索引」「评审记录」三个可独立回退的交付物，并补上正文与关联 | 逐提交单独 revert 后全绿 |

## 转交人类伙伴

- #132 的 blocked-by（#187 / #188 / #195 / #196）需要人类批准（`AGENTS.md` §7），本轮不代写。
- `v1 → undefined` 返回 false：一旦出现带版本的观察，之后不带版本的观察都会被判成乱序。两个实现一致，也符合 capabilities 的「undefined 最小」，但语义归端口层决定。
