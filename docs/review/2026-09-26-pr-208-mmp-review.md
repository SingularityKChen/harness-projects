# PR #208 第二轮 MMP 交付评审记录

> 状态：Completed（无 P0 / P1；就地修复后以 rebase merge 合入）
> 评审日期：2026-09-26（Asia/Shanghai）
> PR：[#208](https://github.com/SingularityKChen/harness-projects/pull/208)，评审时 `Closes #205`，修复后 `Refs #205`
> 关联 ExecPlan：[2026-09-26-development-suite-capability-driven](../exec-plan/completed/2026-09-26-development-suite-capability-driven.md)
> 跨 PR 矩阵与并集预演：[2026-09-26-start-work-stack-round6-review](2026-09-26-start-work-stack-round6-review.md)

## 锁定事实

- 评审 head：`7490197`（1 个提交），base `main@53aadd5`；review id `5325081139`，10 条 inline。
- 评审 head 上的实测（与 PR 描述逐项一致）：`tsc --noEmit` exit 0；`node --test tests/contract tests/integration tests/e2e` → 614 / 614（base 587，+27 全部来自 `development-contract.test.js` 13 → 40）；`node --test tests/contract` → 524 / 524；mvp0 7 / 7；boundaries 7 / 7；size 代码 276 / 1000、文档 180 / 1500。

## 上一轮（第五轮）6 条复核

| # | 位置 | 判定 |
|---|---|---|
| 1 | 家族门 | 变更请求族已修（变异 → 40 / 32 / 8）；同根因在分支 / 工作树族仍在 |
| 2 | 空转断言 | 部分：`fn === undefined` 分支里的断言删掉结果不变，红来自 `TypeError` |
| 3 | 替身判定顺序 | 部分：`createWorktree` / `removeWorktree` 改回身份优先仍 40 / 40 |
| 4 | 证据数字 | 已修；当轮意见里「27」那半句是评审自己错了 |
| 5 | `expect.objects` 进接口面 | 已修 |
| 6 | `deepEqual` → `deepStrictEqual` | 原意见不成立，作者的反驳成立 |

## 本轮意见与处置

| 级别 | 位置 | 意见 | 处置 |
|---|---|---|---|
| P2 | `suites/development.js:193` | 分支 / 工作树未按快照驱动 | **留在 #205**（设计变更，见 ExecPlan Decision Log）；套件头写明现状 |
| P2 | ExecPlan 验收 7 | 栈底 `Closes #205` 不成立 | 改为 `Refs #205`；验收 7 标注「由 #160 验收」 |
| P2 | ExecPlan 判别性期望 | `if (false)` 期望过期 | 改为 40 / 38 / 2，原值标 Superseded |
| P2 | `suites/development.js:69` | `AccessLevel` 四态压成两态 | 最小修：套件头与 port 写明只建模能力声明，不再声称支持 `ReadOnly` |
| P2 | `suites/development.js:85` | 新鲜度只在 `createBranch` 之后 | 工作树创建 / 移除、变更请求创建之后各加一条 `notDeepEqual` |
| P3 | `suites/development.js:56` | 同义反复断言 | 移到已声明路径上的 `implemented()` |
| P3 | `suites/development.js:177` | 谱系用例读 CR 不按读键 | 按 `declares(provider, 'getChangeRequest')` 分支 |
| P3 | ExecPlan「七个文件」 | 实为八个 | 订正 |
| P3 | ExecPlan「当前状态」与轮次 | 写的是 #160 分支上的中间版本 | 改为 `main@53aadd5` 的真实起点；轮次统一为「Start Work 栈第 N 轮」 |
| P3 | ExecPlan 证据与恢复锚点 | 本层不可解析 | 标注证据随 #160、恢复锚点不在远端 |

## 修复后的验证（在检出 `test/development-suite-capability-driven` 的 `.worktrees/pr-a-suite` 根目录运行）

| 命令 | 输出 |
|---|---|
| `./node_modules/.bin/tsc --noEmit` | 无输出，exit 0 |
| `node --test tests/contract/development-contract.test.js` | 40 / 40 / 0 |
| `node --test tests/contract tests/integration tests/e2e` | 614 / 614 / 0 |
| 变异 `if (false)`（移除分支） | 40 / 38 / 2 |
| 变异 家族门（`createChangeRequest` 取 read 键） | 40 / 32 / 8 |
| 变异 省略可选方法的适配器谎报可用 | 40 / 35 / 5，5 条失败都是 `implemented()` 的「就必须实现」 |
| 变异 替身适配器的 `objects` 只返回分支 | 40 / 34 / 6 |
| 修复补丁打到 #160 head `b0e8499` | contract+integration+e2e 638 / 638；`development-local-git.test.js` 23 / 23 |
| 修复补丁打到栈顶 `329a475` | 665 / 665；`development-local-git.test.js` 23 / 23 |

变基到合并时的 `main` 之后的数字与合并记录写在 PR #208 的描述与合并回读里（本记录随该 PR 合入，写不到自身的合并结果）。

## #205 验收（修复后，字面）

| # | 验收 | 判定 |
|---|---|---|
| 1 | 新键存在；未声明它的 provider 不被要求移除 | 成立 |
| 2 | 每个可选方法：缺失 → 不可用；存在但未声明 → `not_supported` | 部分：变更请求三个方法与移除成立；`createBranch` / `createWorktree` 仍按已声明使用 |
| 3 | 缺 `expect.objects` 或返回陈旧快照时套件失败 | 成立（修复后每种已知写入之后都查新鲜度） |
| 4 | 替身与本地 Git provider 都以真实子集通过 | 替身成立；本地 Git 随 #160 |

#205 保持开启。
