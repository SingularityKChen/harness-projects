# PR #160 MMP 交付评审（第七轮，当前 head）

> 评审对象：feat/local-git-worktree 的 immutable head e7136ad6cc748f9a274485f6b7bb0a28dee5ad21，相对 base main@9b33fe52f40b6f2eb6e3677551c41d1fc603b7dd。
> 工作树：.worktrees/w11-d2a；此次只写审查证据，不改生产代码，不提交 GitHub review。
> 结论：Blocked（1 × P1）。P0 未发现；另有 P2/P3 候选，不能用绿 CI 替代产品闭环或路径边界证据。

## 1. 锁定事实与证据边界

- git rev-parse HEAD = e7136ad6cc748f9a274485f6b7bb0a28dee5ad21；git diff --check 9b33fe5...e7136ad 无输出；工作树开始时 clean。
- GitHub PR 160：open，base main@9b33fe5，head e7136ad，作者 SingularityKChen；标签 kind:feat、area:providers、area:tests，未见 gate:*。
- 当前 checks：PR Fast Gate、Verify、Boundaries、Integration、E2E、MVP-0、Disclosure、Issue policy 均 pass；PR size fail。当前本地 rule-checks 实测代码 1014/1000、文档 1170/1500。
- 本地判别性验证：pnpm exec tsc --noEmit 通过；node --test tests/contract tests/integration tests/e2e = 720/720/0；node --test tests/mvp0 = 7/7/0；workflow-check 无 findings；disclosure 机械扫描通过。
- 历史 review threads 已回读。旧 head 的结论只作背景；本记录的阻塞项只针对 e7136ad 当前代码。

## 2. P0–P3 风险矩阵

| 级别 | 层 | 风险、当前证据与反例 | MMP 判定 | 建议验收 / inline 锚点 |
|---|---|---|---|---|
| P0 | 系统 / 发布 | 未发现会直接破坏 main、凭据边界或不可逆数据的 P0。 | 无 P0 | 保持当前发布面扫描与受控 merge 流程。 |
| P1 | 代码 / 安全边界 | paths.ts:100 只在目标未完全存在时调用 firstLink(target.ancestor, candidate)。当 target.ancestor 自身已是符号链接时，firstLink 从其子项开始，漏掉 ancestor。真实 Git 复现：repo/alias -> <allowedRoot>，请求 path=alias/new，allowedRoot 为仓库外允许根；provider 返回 ok=true，worktree add 实际登记 <allowedRoot>/new，runner 有 worktree add 和回读调用。 | 阻塞。违反 AGENTS.md §7 未经允许的符号链接边界与 #137 验收 1。 | 在 paths.ts:100 附近加入“已存在符号链接祖先 + 新尾部”的零 Git 调用用例；应返回 invalid_input。修复 firstLink 使其检查 ancestor 本身或逐分量检查真实边界。 |
| P1 | 产品闭环 / 架构 | #160 当前只交付 provider 自身；经 core 的 provisionGit/startWork 验收拆到叠加 PR #209。provider 测试绿不能证明用户获得 Start Work 的 ready/saved/confirmed 闭环。 | 不单独阻塞 provider 修复，但阻塞整栈 MMP；#209/#161 必须在本 P1 修复后 rebase。 | #209 在当前 #160 head 上复测上述反例，确认 startWork 不报告权威 Saved/confirmed，且链接目标未写入。 |
| P2 | 错误传播 / 状态 | provider.ts:125 对 symbolic-ref 的非 not_found 错误（permission/unavailable）返回 undefined；getRepository() 仍返回 ok=true、defaultBranch=undefined。最小注入：rev-parse --git-dir 成功、symbolic-ref 返回 Permission denied，实际 getRepository 仍成功。core 随后可能回落 main。 | P2，不阻塞本轮，但恢复动作被错误归类。 | baseBranch 返回结构化错误并由 getRepository 传播；补故障注入用例。建议锚点 provider.ts:125。 |
| P2 | 工作项 / 规模 | PR size 当前 1014/1000。说明写了“advisory”，但 AGENTS.md §6/§8 仍要求超限拆成可独立验收批次或由人类裁决。 | 不单独升 P1，但发布记录必须显式保留未达规模门禁。 | 拆出结构扫描/契约批次，或在 ExecPlan Decision Log 写明接受超限的理由、回滚边界和 issue 验收。 |
| P2 | 架构 / 信任边界 | 默认 .worktrees 根的链接已拒绝，但注入仓库外 allowedRoot 与仓库内 target 链接仍由 target ancestor 形状决定；“宿主批准 root”不能自动批准 target 侧链接。 | 与 P1 同根，防止只修默认根后回归。 | 路径契约分别证明 repository、allowedRoot、target 的已存在分量策略；不按派生/注入跳过 target 检查。 |
| P3 | 测试判别力 | branchRecords() 在 provider.ts:303-306 对每行直接 trim；预置的合法 Unicode 空白分支名可能被改写或漏读。 | 候选，不阻塞。 | 若保留精确枚举承诺，增加边界 ref 用例，或只移除记录分隔符而不 trim 名字。 |
| P3 | 测试 / 源码扫描 | 静态扫描覆盖多种 import/require 形态，但运行时拼接模块说明符无法静态捕获；当前无利用证据。 | 候选，不阻塞。 | 后续单独批次使用运行时 mock，或明确静态扫描的非完备边界。 |
| P3 | 文档 / ExecPlan | ExecPlan 包含多轮历史读数；当前 head 的易失值已有回读命令，但归档前仍需逐条核对当前 P1 反例、验收和 Outcomes。 | 候选，不阻塞。 | 归档前重新跑 size、diff --check，并把当前反例与修复 head 写入 Outcomes。 |

## 3. 分层检查结论

### 代码、错误传播、状态与恢复

正常创建、同名分支、重复工作树、大小写变体、能力关闭、Git 写后回读和 ambiguous_result 用例均通过。ensureWorktree 对 conflict 直接确认的跨层风险已由 provider 显式复用判定部分补偿，但链接祖先绕过仍能让 ok 事实落在未批准目标。

baseBranch 的权限失败吞噬是读错误传播缺口：它把“无法观测”变成“成功但无默认分支”，与 provider error 语义和 Host 权威状态不符。

### 产品闭环与真实用户结果

720 个 provider/契约测试只能证明局部能力；#160 不装配 core 的 startWork，真实纵向闭环由 #209 叠加验收。必须把本 P1 反例带到 commands.startWork 级别，确认不会先落盘再报告 Saved/confirmed。

### 架构边界与七条不变量

依赖方向和 provider → capabilities/domain 边界未见反向 import；Git 调用集中在 git-runner.ts，源码扫描通过。路径信任边界仍有 P1：target 的已存在符号链接 ancestor 可把写入导向 allowedRoot 下另一处。info/exclude 是可见本地副作用，正常路径有幂等测试；权限失败需单独验证。

### 测试与反例

主要变异证据有判别力，但缺少“已存在 symlink ancestor + 新尾部”反例，正好对应 firstLink 从 target.ancestor 子项开始的实现形状；也缺少 baseBranch 非 not_found 失败传播用例。不能因 720 个测试全绿而关闭两条机制风险。

### Issue、ExecPlan、规模、标签、closing references、checks

- PR 正文含 Closes #137、Refs #136/#138/#205/#208/#207/#209 与新开 #212/#213/#214；标签满足一个 kind、至少一个 area、无 gate 标签。
- ExecPlan 仍 active；待 P1 修复及 #209/#161 rebase 后，必须重锁 head/base、重跑 checks，再移入 completed 并更新 docs/README.md。
- 代码规模 1014/1000，PR size fail；应拆批或由人类在 Decision Log 明确裁决。
- PR Fast Gate 和功能检查通过，不能抵消 P1 路径事实或本 PR 单独无用户闭环的证据。

## 4. 建议的当前 inline review 集合（不在本子任务提交）

1. P1 blocking，packages/providers/development-local-git/src/paths.ts:100：firstLink(target.ancestor, candidate) 在 target.ancestor 自身是 symlink 时从其子项开始，漏掉已存在 symlink ancestor。反例 repo/alias -> <allowedRoot>，请求 alias/new，真实 worktree add 落到 <allowedRoot>/new 并返回 ok。请在任何 Git 命令前拒绝并补零调用判别测试。
2. P2，packages/providers/development-local-git/src/provider.ts:125：baseBranch 把非 not_found 的读取失败折成 undefined，getRepository 仍返回成功；请传播 permission_denied/unavailable 并补故障注入测试。
3. P2，PR 级：PR size 当前 1014/1000，要求明确拆批或人类裁决；检查结果不能写成全绿。

## 5. 复评门槛

修复 P1 后，在新 immutable head 重新运行：

    git rev-parse HEAD
    git diff --check 9b33fe5...HEAD
    pnpm exec tsc --noEmit
    node --test tests/contract tests/integration tests/e2e
    node --test tests/mvp0
    node scripts/rule-checks.mjs size <declared-base>
    gh pr checks 160 -R SingularityKChen/harness-projects

随后在 #209、#161 逐个 rebase，重新检查链接反例、Start Work 的权威 Saved/confirmed、issue/closing references 和 review threads。任一 rebase、force-push、retarget 或修复提交都会使本矩阵失效，必须以新 immutable head 重建。
## GitHub 提交与当前门禁（2026-09-26）

- 使用 `gh-review`（账号 `Singularity-AI-Bot`）对 immutable head `e7136ad6cc748f9a274485f6b7bb0a28dee5ad21` 提交 consolidated review：review `5325575692`，状态 `CHANGES_REQUESTED`；inline 为 paths.ts P1、provider.ts P2、ExecPlan size P2。
- 当前回读：`gh pr view 160 --json headRefOid,baseRefOid,mergeStateStatus,reviewDecision`；期望 head/base 仍为本记录锁定值，`mergeStateStatus=BLOCKED`、`reviewDecision=CHANGES_REQUESTED`。
- 结论：P1 未修前不合并；不执行 rebase merge、ExecPlan 归档或 P2/P3 收口。

## W2 修复批次（2026-09-26）

- 新增集成反例：仓库内 `alias` 符号链接指向仓库外 `allowedRoot`，请求 `alias/new`；先删除 ancestor 守卫时测试为 **23 / 24**（新增用例失败，解析结果错误地为 `ok: true`）。
- 修复 `packages/providers/development-local-git/src/paths.ts`：`firstLink` 先对 `ancestor` 自身执行 `lstat`，再逐分量检查；恢复后同一聚焦命令为 **24 / 24**，provider 返回 `invalid_input`，注入 runner 的 `calls` 为零。
- 本批次不改变默认 allowedRoot 迁移、不触碰 #209/#161 文件；后续复评仍需在新的 immutable head 上重锁 base/head 并重跑整栈门禁。
