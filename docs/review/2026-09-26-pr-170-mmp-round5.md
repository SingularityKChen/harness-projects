# MMP 交付评审（第五轮）· PR #170 · head `d84bc16` · 结论：**1 × P1（改写历史丢失已验收的判别性用例），本轮不合并**

> 状态：Blocked（本轮不合并）
> 评审日期：2026-09-26（Asia/Shanghai）
> GitHub review：`5319936286`；inline 意见在 PR 上，本记录是 PR 级结论与重新入队条件
> 本记录由评审者写在本工作树，随第六轮修复一并提交（2026-09-26）。下面的**锁定事实是第五轮评审时点的快照**（head `d84bc16` 已被重写，当前 head 以 `gh pr view 170 -R SingularityKChen/harness-projects --json headRefOid` 回读）；第六轮结论见 `docs/review/2026-09-26-pr-170-mmp-round6.md`。批次记录（含全部八个 PR 的矩阵与合并结果）已随 #167 进入 `main`：`docs/review/2026-09-26-mmp-round5-batch-review.md`

**锁定事实**：head `d84bc16792`（4 个提交），base `89d1a9e`（= #167 head），`CLEAN`，12 / 12 checks 通过，`closingIssuesReferences = []`（`Refs #164`）。本合并点 contract 504 / 504、integration 61 / 61、e2e 39 / 39、mvp0 7 / 7；`size` 代码 760 / 1000、文档 788 / 1500。

### 风险矩阵
| 级别 | 条目 |
|---|---|
| P0 | 无 |
| **P1** | `storage-sync.ts:170`：本层声明的 Batch E 闭环不成立。第四轮 head `ae39655` 上存在的 SQLite 身份同步组注册、装配守卫、结算令牌用例与两条同步用例在当前 head 上都不存在；`fd33899` 的正文与 4 条 thread 回复承诺的四条共享用例不存在；Batch E 自己声明的注入实验不变红（611 / 611）。`putMembership` 从 `atomic` 改成 `mutate`、`#committedVersion` 去掉 `object_kind` 都保持 125 / 125 |
| P2 | 7 条 inline：装配守卫消失（不注册 SQLite 同步组时 117 / 117 仍绿）；SQLite 未注册身份同步组而计划当它在跑；P1-a 用例消失（`columnToVersion` 改成恒等仍绿）；结算令牌无用例（删掉检查后泄漏的 `tx` 写入返回 resolved、随下一个事务一起消失）；计划被 L4 订正前的旧文本覆盖；`docs/README.md:49` 仍写「观察落点 = 成员关系」（R4-1 回复称已修） |
| P3 | 9 条 inline（计划里的机械替换改坏了代码 span；`git diff --check 6526ded9` 报 fatal；允许面与 D13 矛盾；`storage-sync.ts` 实为 265 行；其它层的索引行被改回旧文本；游标父边仍分叉；#189 未登记；3 个存活变异；「遗留 5」引用） |

### 第四轮复核
7 条全部只是部分修复，其中 R4-1–R4-5 的作者回复与 head 不符。

### #164 验收（字面）
5 / 5 成立（N 次投递等于一次、重开后仍去重、`v1 → v3 → v2` 为 `[true,true,false]`、后到者胜走端口 upsert、缺父行被拒且不留行、只依赖 domain 与 capabilities），但描述仍写 `Refs #164` 且没有理由。P1 修好后应改 `Closes #164`。

### 产品闭环
core + SQLite 文件库首轮同步与替身逐项一致（5 个视图、账本 5 行），重开后 id 不变、无重复观察，带重复与乱序故障的重同步账本不变。#189（游标只按 binding 取键）在两个实现上同样复现。

### PR 级意见
- **[P2]** 描述损坏：每个字符之间插入 `Refs #164`（1033 次），去掉后是未填充模板。
- **[P3]** `area:capabilities` 标签与 diff 不符。

### 修复方向
从第四轮 head `ae39655`（作者本地 `backup/l5-pre-round4`）取回丢失的装配、守卫与用例，而不是重写；修完后 `Closes #164`。L4 合并后本分支需 `git rebase --onto origin/main <L4 合并前的 head>`。

### 栈级结论（#121 → #122 → #157 → #167 → #170 → #175 共用）

- **并集预演**：一次性 clone 里两栈两种顺序（SQLite 栈 → Start Work 栈、反序）都能变基，唯一的文本冲突是 `docs/README.md` 索引（两边都保留）。并集树上 `pnpm verify` 唯一的红来自 #160 的 5 条本地 Git 集成用例：它的 fixture 没有先 `putWorkspace` 就 `registerBindings`，而 #122 起的替身（与 SQLite 外键一致）要求工作区先存在；core 自己的 `createContext` 总是先建工作区。strict 分支保护保证后合并的一方在含前者的树上重跑必需检查，所以这是**硬性的合并顺序依赖**，不是会打坏 `main` 的 P0；修法落在 #160（见该 PR inline）。栈顶单独跑：625 / 625。
- **P1 分布**：#170 × 1、#175 × 2，根因相同——第四轮之后 L5 / L6 的改写把第四轮 head 上已经存在的内容丢了：SQLite 的身份同步组与执行组装配、装配守卫、结算令牌用例、两条同步用例、`replacePlanningProjections` 的 `atomic` 与钉住它的重启用例。第四轮 head `ae39655`（L5）/ `693aba4`（L6）以及作者本地的 `backup/l6-wip`（`a3c46b8`（作者本地备份，远端不可解析））里都还有这些内容，可以从那里取回，而不是重写。
- **L1–L4 无 P0 / P1**：按人类伙伴本轮授权（无 P0 / P1 代码问题时修复 P2 / P3、归档计划、整合提交后 rebase merge），评审者就地修能机械修的 P2 / P3；会扩大范围、超出体量上限或需要人类裁决的条目登记 issue。自下而上合并，每合并一层重锁下一层的 head / base 并重跑验证。
- **L5 / L6 本轮不合并**：L4 合并后，两层按 `git rebase --onto origin/main <L4 合并前的 head> <分支>` 重放，再修 P1。
- **产品闭环**：没有宿主或 app import `storage-sqlite`，整栈合并后用户观察不到变化；core + SQLite 文件库首轮同步与替身逐项一致、重启后 id 不变；Start Work 在 SQLite 上抛裸 `FOREIGN KEY constraint failed`（#187 / #188 / #195 / #196 跟踪，把它们挂成 #132 的 blocker 需要人类批准）。没有任何文档宣称 Gate E1 已通过或数据模型 v1 已冻结。
- **全栈 PR 描述**：#122 / #157 / #170 的描述每个字符之间都插了一次 `Refs #N`（模板替换缺陷），#121 / #167 留着 `BATCH` / `REL` / `EVIDENCE` / `RISK` / `HEAD` 占位符，#175 的 head 与数字过期——`AGENTS.md` §8 要求的验证证据、风险与关联节在六个 PR 里都不可读或缺失（PR 级 P2，合并前必修）。

## 重新入队条件

1. 从第四轮 head `ae39655`（作者本地 `backup/l5-pre-round4`）取回丢失的 SQLite 身份同步组注册、装配守卫、结算令牌用例与两条同步用例；每取回一条，先让它在当前实现上通过，再用对应变异证明它会变红。
2. 按 `git rebase --onto origin/main 89d1a9ebcd40e1f47671a3b1c18197ac882b79a5 feature/storage-sqlite-sync-surface` 重放到含 L1–L4 的新 `main`（L4 合并前的 head 是 `89d1a9e`）。预期冲突：`docs/README.md`（两边都保留）、收敛计划「本层实测」行（改成 `gh pr view 170`）、`suites/storage-sync.js:96` 的消息与断言基线（L4 已恢复原文）。
3. 修完后改 `Closes #164`（五条验收字面成立），并按 `AGENTS.md` §8 重写描述（当前描述每个字符之间插入了 `Refs #164`）。
4. 重写后逐文件核对本层改动与重写前逐字相同，只有有意的修复不同——这正是本轮 P1 的根因。
