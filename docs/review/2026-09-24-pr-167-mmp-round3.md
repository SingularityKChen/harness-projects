# PR #167 MMP 交付评审记录（第三轮）

> 评审日期：2026-09-24（Asia/Shanghai）
> PR：[#167](https://github.com/SingularityKChen/harness-projects/pull/167)
> GitHub review：`5300232026`（9 条 inline 加 PR 级总结，事件为 COMMENT：作者与评审者是同一账户，不能 request changes）
> 本记录随 #167 第四轮修复提交；锁定事实保留第三轮观察时刻（`1773f99` / `d4a8bb9`，不可回读），当前 head / base 用 `gh pr view 167 -R SingularityKChen/harness-projects --json headRefOid,baseRefOid` 回读。

## 结论 · MMP 交付评审（第三轮）· PR #167 · head `1773f99` · 结论：**Blocked（1 × P1；下层另有 P1）**

**锁定事实**：head `1773f9900eb8`（7 个提交），base = #157 head `d4a8bb9`，`CLEAN`，11/11 checks 通过，`Closes #163`。本合并点实测：contract 486/486、integration 30/30、e2e 38/38、mvp0 7/7；`size d4a8bb9` 结果为代码 **1000/1000**、文档 445/1500。

### 风险矩阵
| 级别 | 条目 |
|---|---|
| P0 | 无 |
| **P1** | **级联切分丢掉了 L2 钉住的两条默认降级断言**（inline `storage-foundation.js:78`）。主控复核：`git grep "降级只改 isDefault"` 在 base `d4a8bb9` 上命中 1 处，从 `9642c92` 起为 0。保留下来的那条能证明旧默认被降级，但证明不了「降级不改启用状态、不删行」：把降级改成「降级并禁用」或「直接删除」，现有套件仍然 50/50 绿。#163 把 demote 规则列在 Scope 内，并写明 *no case may be dropped or relaxed*。 |
| P2 | 3 条 inline：泄漏的 `tx` 会绕开队列、并入别的事务，回滚后写入丢失，调用方却拿到 resolved；证据基准 `8f11e38` 不是 head 的祖先（`size` exit 1）；验收 6 在本层没有经端口写入的用例（跨层评审）。另有 PR 级 1 条，见下 |
| P3 | 5 条 inline：ALS 作用域在结算后不失效；`close()` 的快速失败覆盖不全；头注释漏了两处分叉；repository / revision 引用完整性遗留在计划里消失；「002/003 已冻结」的措辞 |

### 上一轮复核
- P1「读路径与写队列不等价」：**部分成立**。脏读已修（变异 M1 去掉串行后 3 条变红），残留见 P2 第 1 条。
- P2 证据基准：**不成立（复发）**。P2 条数账：**成立**。
- P3 `close()`：**部分成立**。作者说 `close()` 已排进队列，与代码不符。
- P3「work 里调外层实例自等」：**部分成立**。SQLite 已快速失败；替身仍会挂起；作者说「共享用例」，实际只在 SQLite 专属区。
- 列表顺序、错误文本、投影 UPSERT：**成立**。

### #163 验收（字面）
| # | 判定 |
|---|---|
| 1 两个实现跑地基组 | 成立 |
| 2 分组合计等于原套件（守卫） | 字面成立（守卫只数 `register(`）；但违反 Out of scope「no case may be relaxed」→ P1 |
| 3 抛错事务不留行 | 成立 |
| 4 嵌套事务被拒并钉住 | 成立（M2a / M2b 各红） |
| 5 重启用例 | 成立 |
| 6 端口写过的库重跑迁移 | 行为成立；**用例没有写入** → P2 |
| 7 只 import domain / capabilities | 成立 |

### PR 级意见
- **[P2] PR 描述过期，闭环声明不成立**：描述仍写 head `6526ded`、base `a646073`、999/1000、422 条。「core 的 bootstrap 路径可以跑在真实文件库上」不成立：在 SQLite 上 `composeCore` 报 `not implemented in L4: getSyncCursor`。#163 Notes 的同一句话在任何 head 上都不成立。
- **[P3] 提交 `1773f99` 的信息与 diff 不符**：它只改了注释，那五条断言其实在 `9642c92` 里。
- **[P3]** `area:capabilities` 标签与改动面不符。
- **预算**：修 P1 要净增 3 行，代码已经 1000/1000，需要同批压缩；验收 6 的用例应当替换而不是新增。

### 栈级结论（六个 PR 共用）

- **没有 P0**。主控在一次性 clone 里按栈序逐个检出六个合并点（`main@484f0d3` → `b6e8a16` → `5f0db7c` → `d4a8bb9` → `1773f99` → `fc4dd90` → `e3586e6`），每个点都跑了 typecheck、contract、integration、e2e、mvp0、boundaries、workflow-check、disclosure、size 与 `git diff --check`，全部通过。没有任何文档宣布数据模型 v1 已冻结或 Gate E1 已通过：裁决 §3 仍是 `revise`，行为 6 的改判写明了待人类伙伴采纳。
- **阻塞的 P1 一共 5 条，都经主控独立复现**：#121 ×1（控制计划在栈底把 L2–L6 写成已交付）、#122 ×2（条目移出再移回后 core 同步永久抛错；`Closes #27` 但验收 3 不成立）、#157 ×1（`Closes #28`，但重启验收在本层不可能成立）、#167 ×1（切分契约套件时丢掉了 #163 明令不得放宽的断言）。
- **子评审报的 P1 里有 3 条被主控降为 P2**，理由写在各条意见末尾：#157 的跨工作区作用域（在 base 上同样复现，根因在 `main` 的端口）、#157 的账本键（本层合并点没有写者）、#175 的 `Closes #5`（验收在端口层字面成立，缺口在于没有 issue 跟踪）。
- **合并判定：本轮一个都不合并。** 栈只能自下而上合并，#121 有 P1，所以 #170 和 #175 虽然本层没有 P1，也不能合并。
- **修复后重新排队**：先修 #121 → #122 → #157 → #167 的 P1，再按栈序级联，每层重新锁定 head、重跑本层验证。同时刷新六个 PR 的描述：六份都引用了已不可解析的 head、过期的计数和错误的回滚提交数。

## 评审方法与边界

- **流程**：先锁定 head/base、全部 thread 与 checks → 按栈序在一次性 clone 里逐合并点跑门禁（并集先于逐 PR）→ 6 个逐层子评审加 1 个跨层子评审并行取证，都只读，用 Opus（任务需要对 schema、事务并发和文档时点事实做对抗性推理）→ 主控独立复现每一条阻塞级结论，并裁定级别 → 74 条 inline 连同 6 份总结一次性提交。
- **复评原则**：上一轮每条意见都在当前 head 上复跑反例或变异，thread 的 resolve 状态和作者回复都不当作证据。每次变异都先打印被改的那一行，证明变异确实生效，还原后再复跑一次确认回绿。
- **主控对子评审定级的调整**：报成 P1、被降为 P2 的有三条——#157 跨工作区作用域（base 上同样复现，根因在 `main` 的端口）、#157 账本键（本层合并点没有写者）、#175 `Closes #5`（验收在端口层字面成立，缺口是没有 issue 跟踪）。另有一条报成 P1、按上一轮口径维持 P2 的：#121 #119 验收 1 的墙钟时间（没有新事实）。#122 验收 3 那条，主控一度考虑降级，但 #27 Scope 要求把不变量编码进数据库，最终维持 P1。
- **边界**：没有重跑沙箱实验（那会写外部仓库）；CI 只读了 conclusions，没有打开日志；看板字段没有代写（上一轮手动写 `Changes requested` 与投影不一致，被观察者报成漂移）。子评审的实验脚本留在一次性 clone 里，没有进仓库。
