# PR #121 MMP 交付评审记录（后续轮）

> 状态：Blocked（1 × P1，本轮不合并；#122 叠在其上，同样不合并）
> 评审日期：2026-09-24（Asia/Shanghai）
> PR：[#121](https://github.com/SingularityKChen/harness-projects/pull/121)，声明 Closes #119，Refs #120 #27 #28 #5 #4
> 本记录由评审者写在 `.worktrees/w7-e1-5`，未提交；作者修复 P1 时随修复一并提交，并按新 head 更新"锁定事实"。
> GitHub review：`5298680079`（11 条 inline + PR 级总结）

## 锁定事实

- head `74086b2ee4df`（5 个提交），PR 记录的 base `main@940046ec15c3`；评审时 `origin/main` 为 `484f0d3`，本 PR `mergeable = CONFLICTING`。
- 评审 head 上的实测（在检出 `test/e1-uncertain-create` 的 `.worktrees/w7-e1-5` 根目录运行）：`node --test tests/contract/e1-evidence-consistency.test.js` → 4 / 4；`node --test tests/contract` → 404 / 404；`node scripts/rule-checks.mjs size origin/main` → 代码 137、文档 1396；disclosure 通过；`git diff --check origin/main...HEAD` 无输出。
- 栈：#121 → #122 → #157 → #167 → #170 → #175。评审时 #170 不在 #167 的 head 之上（另一会话正在级联上层）。

## 并集实测（先于逐 PR 评审）

在一次性 clone 里把 #121 变基到 `origin/main@484f0d3`，再用 `rebase --onto` 叠上 #122：

- 唯一冲突：`docs/project-management/merge-queue.md`，main 的 `### 4.7` 与本 PR 的 `### 4.5` / `### 4.6` 落在同一插入点，两边都是纯新增；按 §4.7 已写明的解法三节并存（4.5、4.6、4.7）。
- 并集树：`pnpm verify` → 508 / 508 + 7 / 7；`node --test tests/integration` → 15 / 15；`pnpm run boundaries` → 7 / 7；`node scripts/workflow-check.mjs` → 无发现；`git diff --check`、disclosure 通过。**并集层无 P0。**
- 变基后本层文档体量 1396 / 1500（PR 描述写的 1497 来自一个已不可解析的旧 head）。

## P1（阻塞）与独立复现

**栈底把未合并、未评审的上层实现写进 `main` 的 Gate E1 裁决、控制计划与文档索引。** `docs/architecture/gate-e1-ruling.md:116`（R4）与 `:120`（R8）在最后一个提交 `74086b2` 里追加了 "Superseded by L5：003 的实际主键是 …" 与 "Superseded by L3：003 建的是 `mutation_attempt` …"；控制计划 `:24` 写 "L2–L6 的实现面与验收表 1–11 项已全部通过"，`docs/README.md:43` 写 "L1–L6 均已交付"。

```text
$ git ls-tree --name-only origin/test/e1-uncertain-create packages/storage/sqlite/migrations/
001_init.sql
$ git ls-tree --name-only origin/feature/storage-identity-membership-tables packages/storage/sqlite/migrations/
001_init.sql  002_identity_membership.sql
$ git ls-tree --name-only origin/feature/storage-execution-relation-write-tables packages/storage/sqlite/migrations/
001_init.sql  002_identity_membership.sql  003_control_facts.sql
```

迁移 003 只在仍 OPEN 的 #157 上。按栈序合并后，`main` 的门禁权威文档会把 R4、R8 标为"已被取代"，而取代它们的代码不在 `main`、也没有通过评审；R4 行也不在 #119 授权的修订范围内。不是 P0：§3 汇总结论仍 `revise`、仍写 v1 "不满足冻结条件"，采纳权留在人类伙伴。

## #119 验收

| # | 判定 |
|---|---|
| 1 九字段 + 墙钟时间与确切命令 | 部分：3(a) 对账与标签回读缺墙钟时间；实验 4 记录的轮询命令不是产生输出的那条 |
| 2 重复创建计数有测量 | 满足 |
| 3 对账查询与两个分支 | 满足（按字面） |
| 4 §2.6 新裁决与 R8 | 部分：R8 同一行两套状态集；R4 行越权修改 |
| 5 沙箱 fixture 表 | 满足 |
| 6 disclosure 与 `git diff --check` | 满足 |

## P2 / P3

逐条见 GitHub review `5298680079`：R8 两套状态集与支持表夸大；记录的命令产不出记录的输出；控制计划与证据同处一个 PR、预算压缩删掉了上一轮第 8 条的修复；PR 描述证据过期与 `#109`（gh-stack 栈号，不是 issue）；守卫仍有 4 种逃逸（`##` 标题、改名、计划发现、变量改绑）；栈序表 head 过期且不可复核；§4.6 一格与脚本矛盾；未证明清单漏 3 项；改判行未写"待人类采纳"；`kind:docs` 与 #119 的 `kind:test` 不一致。

## 重新进入排队的条件

- 撤掉 R4 / R8 的上层注记（分别随 #170、#157 落地，写成"偏离，待人类采纳"）；控制计划与索引只写本层合并时为真的事实，或把控制计划拆成独立的 docs PR。
- 变基到当前 `main`（`merge-queue.md` 三节并存），重新锁定 head、重跑并集，再按栈序级联 #122 → #175。

## 看板写入记录（`AGENTS.md` §7）

- 2026-09-24T01:55:20Z，actor = agent（按人类伙伴在会话中的明确指令代写），目标 project 10（`PVT_kwHOAY1ahM4BjzAQ`）条目 `PVTI_lAHOAY1ahM4BjzAQzg8Pyvw`（issue #119）字段 `Engineering`（`PVTSSF_lAHOAY1ahM4BjzAQzhiqjsA`）：`PR open` → `Changes requested`（`0d1cbcb3`），幂等键 `clientMutationId = engineering-changes-requested-2026-09-24:PVTI_lAHOAY1ahM4BjzAQzg8Pyvw`；回读 `Engineering=Changes requested`、`Status=In Review` 未变。
- **这是一次与投影不一致的手动写入**：#121 的 `reviewDecision` 为空（评审者与作者是同一账户，GitHub 不允许 request changes，本轮 review 只能是 COMMENT），`stateForSnapshot` 的投影是 `PR open`。写入后只读观察者 `check-engineering-drift-live.mjs --json` 立即报出 `expected: 'PR open', actual: 'Changes requested'`；#121 的下一次生命周期事件会让写入口 reconcile 回 `PR open`。让它持久成立的途径是由作者以外的评审者提交 Request changes。

## 作者响应（第三轮，2026-09-24）

本记录随本层进入 `main`，因此「Blocked / 未提交」是**评审时点**的结论。第三轮的 14 / 20 条 inline 已逐条处置，理由见 `docs/exec-plan/completed/2026-09-24-review-root-cause-convergence.md` 的逐条处置表；本记录的「锁定事实」不再写死 head 与计数，改为回读命令。回读：`gh pr view 121 -R SingularityKChen/harness-projects --json headRefOid,baseRefName`；`node --test tests/contract tests/integration tests/e2e tests/mvp0`。
