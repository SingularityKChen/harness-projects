# PR #121 MMP 交付评审记录（第三轮）

> 评审日期：2026-09-24（Asia/Shanghai）
> PR：[#121](https://github.com/SingularityKChen/harness-projects/pull/121)
> GitHub review：`5300230156`（14 条 inline 加 PR 级总结，事件为 COMMENT：作者与评审者是同一账户，不能 request changes）
> 本记录由评审者写在 `.worktrees/w7-e1-5`，随 L1 的 `docs(review)` 提交归档（**2026-09-24 评审订正**：原文写"未提交"，而它已经在本层的 head 上）。「锁定事实」是**评审时点的快照**，不随新 head 更新——要判当前状态一律用回读命令。**作者响应**见文末。

## 结论 · MMP 交付评审（第三轮）· PR #121 · head `b6e8a16` · 结论：**Blocked（1 × P1）**

**锁定事实（评审时点快照 `b6e8a16`；**订正 2026-09-24（作者响应）**：head 与计数都是易失值，按 `PLANS.md` §4 改成回读命令，不再写死）**：`gh pr view 121 -R SingularityKChen/harness-projects --json headRefOid,baseRefName,mergeStateStatus`；`gh pr checks 121`（期望全 pass）；本合并点的用例数用 `node --test tests/contract`、`node --test tests/integration`、`node --test tests/e2e tests/mvp0`、`node --test tests/contract/package-boundaries.test.js` 回读；体量用 `node scripts/rule-checks.mjs size origin/main`。评审时点的快照值（**只对 `b6e8a16` 那一刻成立，不可当作当前状态**）：base `484f0d3`、`MERGEABLE / CLEAN`、checks 12/12 全 pass、PR 当时写 `Closes #119`；contract 452/452、integration 5/5、e2e 38/38、mvp0 7/7、boundaries 7/7；代码 251/1000、文档 1486/1500；disclosure 与 `git diff --check` 无发现。

### 风险矩阵
| 级别 | 条目 |
|---|---|
| P0 | 无 |
| **P1** | 控制计划 `Progress` 在栈底把 L2–L6 各批标为 `[x]`，并引用本层不存在的 ADR-0005、003、L5 的 `dedupe_key` 与「桩模块删除」（inline `sqlite-v1-stack.md:321`）。主控独立复现：`git cat-file -e b6e8a16:docs/adr/ADR-0005-…` 与 `…/002_identity_membership.sql` 都不存在。这是上一轮 P1 的残留：作者的判定命令只 grep `Superseded by L[2-6]`，所以它漏掉了这一处。 |
| P2 | #119 验收 1 缺两处墙钟时间与确切命令（主控定级见 inline）；R8 的 `pending` / `unknown` 边界自相矛盾；ADR 索引与归档计划仍写「4 个取值」；控制计划 `:428` 仍描述 L5/L6 的遗留编号；两条 PR 级意见见下 |
| P3 | 9 条 inline：守卫仍有 M6/M7/M8 三种逃逸、size 基线写的是分支名（exit 3）、D4 与勾选矛盾、验证块依赖不可解析的 SHA、§4.6 算术、「待人类采纳」只写了一处、未证明清单第 12 条、「每 2 秒一轮」、文件所有权 |

### 上一轮复核（在当前 head 复跑，不采信 resolve 状态）
- 第二轮 11 条：R2-2、R2-10 成立。R2-1、R2-3、R2-4、R2-5、R2-6、R2-7、R2-8、R2-9、R2-11 部分成立：裁决已清干净，控制计划还没有；守卫对改名、升级、降级、删节、多节、重复编号都会变红，但对记录内变量改绑、本层计划都仍是绿。
- PR 描述过期、`kind:docs` 与 #119 的 `kind:test` 不一致这两条不成立（没有修）。
- 第一轮 9 条全部成立。

### #119 验收（字面）
| # | 判定 |
|---|---|
| 1 | **不满足**：3(a) 的对账与标签回读没有墙钟时间，3(a)/3(c) 只写「与实验 2 同形」。按上一轮口径定为 P2，但合并前必须补观测、由人类收窄，或改成 `Refs` |
| 2 | 满足（`count: 2`，附计数用的查询） |
| 3 | 满足（按字面） |
| 4 | 满足（边界问题见 P2） |
| 5 | 满足 |
| 6 | 满足 |

### PR 级意见（没有可锚定的新增行）
- **[P2] 提交序列**：`21ece2a` 引入了 P1 文本，随后由 `bc06b39`、`188d7b5`、`b6e8a16` 撤回。单独 revert `bc06b39` 会把 `Superseded by L5/L3` 带回裁决。rebase merge 会把这些中间态与过期的提交信息带进 `main`，违反 `AGENTS.md` §6「整理成可独立回滚的交付物」。建议按 `git-expert-operations` 先建 backup ref，收敛成约 4 个提交后再级联。
- **[P2] PR 描述过期**：仍写 head `d26cfe7`、「4 个提交」（实际 9 个）、「四层栈」和 `#109`（这是 gh-stack 的栈号，不是 issue）。
- **[P3]** `46fb762` 的标题是 `test(providers)`，但它只改了 `docs/**`；标签与 #119 的 kind 不一致。
- **给人类伙伴的结构性建议**：控制计划和 #119 的证据挤在同一个 PR 里，文档预算只剩 14 行，评审响应计划和评审记录只能随 L2 进仓库，于是本层文档出现了指向 L2 才有的文件的链接。可以考虑把跨层控制计划拆成独立的 docs PR。

### 栈级结论（六个 PR 共用）

- **没有 P0**。主控在一次性 clone 里按栈序逐个检出六个合并点（`main@484f0d3` → `b6e8a16` → `5f0db7c` → `d4a8bb9` → `1773f99` → `fc4dd90` → `e3586e6`），每个点都跑了 typecheck、contract、integration、e2e、mvp0、boundaries、workflow-check、disclosure、size 与 `git diff --check`，全部通过。没有任何文档宣布数据模型 v1 已冻结或 Gate E1 已通过：裁决 §3 仍是 `revise`，行为 6 的改判写明了待人类伙伴采纳。
- **阻塞的 P1 一共 5 条，都经主控独立复现**：#121 ×1（控制计划在栈底把 L2–L6 写成已交付）、#122 ×2（条目移出再移回后 core 同步永久抛错；`Closes #27` 但验收 3 不成立）、#157 ×1（`Closes #28`，但重启验收在本层不可能成立）、#167 ×1（切分契约套件时丢掉了 #163 明令不得放宽的断言）。
- **子评审报的 P1 里有 3 条被主控降为 P2**，理由写在各条意见末尾：#157 的跨工作区作用域（在 base 上同样复现，根因在 `main` 的端口）、#157 的账本键（本层合并点没有写者）、#175 的 `Closes #5`（验收在端口层字面成立，缺口在于没有 issue 跟踪）。
- **合并判定：本轮一个都不合并。** 栈只能自下而上合并，#121 有 P1，所以 #170 和 #175 虽然本层没有 P1，也不能合并。
- **修复后重新排队**：先修 #121 → #122 → #157 → #167 的 P1，再按栈序级联，每层重新锁定 head、重跑本层验证。同时刷新六个 PR 的描述：六份都引用了已不可解析的 head、过期的计数和错误的回滚提交数。

## 评审方法与边界

- **流程**：先锁定 head/base、全部 thread 与 checks → 按栈序在一次性 clone 里逐合并点跑门禁（并集先于逐 PR）→ 6 个逐层子评审加 1 个跨层子评审并行取证，都只读，用 Opus（任务需要对 schema、事务并发和文档时点事实做对抗性推理）→ 主控独立复现每一条阻塞级结论，并裁定级别 → 74 条 inline 连同 6 份总结一次性提交。
- **复评原则**：上一轮每条意见都在当前 head 上复跑反例或变异，thread 的 resolve 状态和作者回复都不当作证据。每次变异都先打印被改的那一行，证明变异确实生效，还原后再复跑一次确认回绿。
- **主控对子评审定级的调整**：报成 P1、被降为 P2 的有三条——#157 跨工作区作用域（base 上同样复现，根因在 `main` 的端口）、#157 账本键（本层合并点没有写者）、#175 `Closes #5`（验收在端口层字面成立，缺口是没有 issue 跟踪）。另有一条报成 P1、按上一轮口径维持 P2 的：#121 #119 验收 1 的墙钟时间（没有新事实）。#122 验收 3 那条，主控一度考虑降级，但 #27 Scope 要求把不变量编码进数据库，最终维持 P1。
- **边界**：没有重跑沙箱实验（那会写外部仓库）；CI 只读了 conclusions，没有打开日志；看板字段没有代写（上一轮手动写 `Changes requested` 与投影不一致，被观察者报成漂移）。子评审的实验脚本留在一次性 clone 里，没有进仓库。

## 作者响应（第三轮，2026-09-24）

**第四轮评审的复核结论是：本轮 14 条里有相当一部分没有落地**（`sqlite-v1-stack.md` 的 `:294`/`:381`/`:376`、`Closes #119`、文件所有权清单、R4 行尾句号等）。第四轮的逐条判定与处置见 **PR #122** 携带的 `docs/exec-plan/active/2026-09-24-review-root-cause-convergence.md`（该文件随 L2 合入，本层只写 PR 引用）。**2026-09-24 评审订正**：原文写"14 条 inline 全部处置"，与当时的 head 不符。本条保留原文以记录当时的判断。P1 的修法是撤掉栈底对上层的事实断言；证据基线从"写死的值"改成"回读命令 + 期望"——这一类前两轮各修过一次仍复发，根因是修的是**值**而不是**判据**。`Closes #119` 改为 `Refs #119`。回读：`node --test tests/contract`、`node scripts/rule-checks.mjs size origin/main`。

**第四轮补充（2026-09-24）**：上一轮声称收口的两个守卫逃逸**当时没有收口**（引号 / `export` / 缩进的 `NAME=ID`、沙箱绑定未进 `localNamesById`），本轮才真正修掉，并有四个变异实测（M6q / M6x / M6i / M9 全部变红，还原后 5 pass / 0 fail）。这条"回复称已修、实际未修"是本轮把处置表换成**能失败的检查**的直接原因：新增 `tests/contract/plan-facts-consistency.test.js` 判定三类复发过的规则（字面 `Closes #N`、写成值的体量基线、不可解析的跨层引用），三个注入实验各自变红。
