# PR #157 MMP 交付评审记录（第三轮）

> 评审日期：2026-09-24（Asia/Shanghai）
> PR：[#157](https://github.com/SingularityKChen/harness-projects/pull/157)
> GitHub review：`5300231409`（12 条 inline 加 PR 级总结，事件为 COMMENT：作者与评审者是同一账户，不能 request changes）
> 本记录由评审者写在 `.worktrees/w9-l3`，**未提交**。作者修复时随修复一并提交，并按新 head 更新「锁定事实」。若本层文档预算不够（#121 已是 1486/1500），与上一轮一样随上一层提交。

## 结论 · MMP 交付评审（第三轮）· PR #157 · head `d4a8bb9` · 结论：**Blocked（1 × P1；下层另有 P1）**

**锁定事实**：head `d4a8bb9c714e`（5 个提交），base = #122 head `5f0db7c`，`CLEAN`，11/11 checks 通过，`Closes #28`。本合并点实测：contract 465/465、integration 27/27、e2e 38/38、mvp0 7/7；`size 5f0db7c` 结果为代码 439/1000、文档 186/1500。

### 风险矩阵
| 级别 | 条目 |
|---|---|
| P0 | 无 |
| **P1** | **`Closes #28`，但验收 1（重启后身份、关系、执行上下文逐字节恢复）在本层不可能成立**（inline `storage-control-facts.md:104`）。主控复核：`git ls-tree d4a8bb9 packages/storage/sqlite/src` 里没有 `storage.ts`，也没有重启用例；控制计划自己的验收表把这几条排在 L4–L6。验收 3、5 同理。本层一合并就会自动关闭 #28。 |
| P2 | 7 条 inline：**级联后以 `binding_id` 为键的事实从「工作区」变成「连接」**（子评审报 P1，主控降为 P2：在 base 上同样复现，根因在端口，意见放在 #122）；跨工作区串号可以落库；账本键与 `updated_at` 可空性（子评审报 P1，主控降为 P2：本层合并点没有写者）；`mutation_attempt` 三种模型互斥；去掉获知方式轴后无法承载 ack/reconcile 证据；替身仍用 `localeCompare`；16 条 DDL 变异里 11 条存活 |
| P3 | 4 条 inline：`003:89` 的「同一判据」不成立（UTF-8 字节序与 UTF-16 码元序不同）；计划残留过期事实；「两个实现共用」在本层不成立；获知方式在 L1 与 L3 用了两套名字 |

### 上一轮复核
- 成立 3 条：D8 出处断言、对不存在的 §9 的引用、计划记录了不存在的测试。
- 不成立 4 条：账本 `dedupe_key`、`updated_at NOT NULL`、`localeCompare`、`mutation_attempt` 模型冲突。作者回复「failed 可经端口重试」，但端口注释、替身、套件和 core 都没改。
- 部分成立 1 条：committed 主体。本层内多绑定已成立；跨工作区不成立；兜底的 tie-break 在本层主键下不可能触发。
- 不适用 1 条：外键与共享套件（本层没有 SQLite 适配器）。

### #28 验收（字面，本层合并点）
| # | 判定 |
|---|---|
| 1 重启逐字节恢复 | **不满足** → P1 |
| 2 关系不能引用不存在的实体 | 部分（有外键，但去掉后用例仍绿；替身接受 `ghost-a`） |
| 3 规则发现的关系落候选表 | 部分（确认表可以直接写入 `deterministic`；本层没有写者） |
| 4 写尝试记录 binding、幂等键与结果 | 满足（偏弱：binding 现在指连接；约束没有判别性用例） |
| 5 失败事务不留行 | 部分（只有替身用例） |
| 6 迁移从空库可重复 | 满足 |

### PR 级意见
- **[P2] PR 描述过期**：仍写 head `19f931e`、L2 `ad76def`、「layer 4 of stack #109」、23/409（实际 27/465）；它说遗留 4「重要」，但计划里已经收口；「四态」与 D14 的五个 `WriteState` 矛盾。
- **[P3] 标签**：`area:domain` 没有对应改动；#28 带 `gate:E1`，本 PR 没带。
- **修复 P1 时注意**：`merge-queue.md` §4.4 记录过，栈内 PR 的关闭引用机制「未确定」。改成 `Refs #28` 后要回读 `closingIssuesReferences`。

### 栈级结论（六个 PR 共用）

- **没有 P0**。主控在一次性 clone 里按栈序逐个检出六个合并点（`main@484f0d3` → `b6e8a16` → `5f0db7c` → `d4a8bb9` → `1773f99` → `fc4dd90` → `e3586e6`），每个点都跑了 typecheck、contract、integration、e2e、mvp0、boundaries、workflow-check、disclosure、size 与 `git diff --check`，全部通过。没有任何文档宣布数据模型 v1 已冻结或 Gate E1 已通过：裁决 §3 仍是 `revise`，行为 6 的改判写明了待人类伙伴采纳。
- **阻塞的 P1 一共 5 条，都经主控独立复现**：#121 ×1（控制计划在栈底把 L2–L6 写成已交付）、#122 ×2（条目移出再移回后 core 同步永久抛错；`Closes #27` 但验收 3 不成立）、#157 ×1（`Closes #28`，但重启验收在本层不可能成立）、#167 ×1（切分契约套件时丢掉了 #163 明令不得放宽的断言）。
- **子评审报的 P1 里有 3 条被主控降为 P2**，理由写在各条意见末尾：#157 的跨工作区作用域（在 base 上同样复现，根因在 `main` 的端口）、#157 的账本键（本层合并点没有写者）、#175 的 `Closes #5`（验收在端口层字面成立，缺口在于没有 issue 跟踪）。
- **合并判定：本轮一个都不合并。** 栈只能自下而上合并，#121 有 P1，所以 #170 和 #175 虽然本层没有 P1，也不能合并。
- **修复后重新排队**：先修 #121 → #122 → #157 → #167 的 P1，再按栈序级联，每层重新锁定 head、重跑本层验证。同时刷新六个 PR 的描述：六份都引用了已不可解析的 head、过期的计数和错误的回滚提交数。

## 评审方法与边界

- **流程**：先锁定 head/base、全部 thread 与 checks → 按栈序在一次性 clone 里逐合并点跑门禁（并集先于逐 PR）→ 6 个逐层子评审加 1 个跨层子评审并行取证，都只读，用 Opus（任务需要对 schema、事务并发和文档时点事实做对抗性推理）→ 主控独立复现每一条阻塞级结论，并裁定级别 → 74 条 inline 连同 6 份总结一次性提交。
- **复评原则**：上一轮每条意见都在当前 head 上复跑反例或变异，thread 的 resolve 状态和作者回复都不当作证据。每次变异都先打印被改的那一行，证明变异确实生效，还原后再复跑一次确认回绿。
- **主控对子评审定级的调整**：报成 P1、被降为 P2 的有三条——#157 跨工作区作用域（base 上同样复现，根因在 `main` 的端口）、#157 账本键（本层合并点没有写者）、#175 `Closes #5`（验收在端口层字面成立，缺口是没有 issue 跟踪）。另有一条报成 P1、按上一轮口径维持 P2 的：#121 #119 验收 1 的墙钟时间（没有新事实）。#122 验收 3 那条，主控一度考虑降级，但 #27 Scope 要求把不变量编码进数据库，最终维持 P1。
- **边界**：没有重跑沙箱实验（那会写外部仓库）；CI 只读了 conclusions，没有打开日志；看板字段没有代写（上一轮手动写 `Changes requested` 与投影不一致，被观察者报成漂移）。子评审的实验脚本留在一次性 clone 里，没有进仓库。
