# PR #209 第二轮 MMP 交付评审记录

> 状态：本层无 P0 / P1；受 #160 的 P1 阻塞，本轮不合并。**本轮的 1 条 P2 与 1 条 PR 级 P3 已在第六轮响应中
> 处置**（见文末「第六轮响应后的锁定事实」；评审原文保留为历史）。本记录由评审者写在作者的工作区、未提交；
> 作者收口时随修复一并提交。
> 评审日期：2026-09-26（Asia/Shanghai）
> PR：[#209](https://github.com/SingularityKChen/harness-projects/pull/209)，Refs #207
> 关联 ExecPlan：`docs/exec-plan/active/2026-09-24-local-git-worktree.md`（与 #160 共用）
> 跨 PR 矩阵：`docs/review/2026-09-26-start-work-stack-round6-review.md`（随 #208 进入 `main`）

## 锁定事实

- 评审 head：`91bd14d`（1 个提交），base = #160 head `b0e8499`；review id `5325081532`，1 条 inline（P2）+ 1 条 PR 级 P3。
- 实测与描述逐项一致：本文件 6 / 6；contract + integration + e2e 644 / 644；mvp0 7 / 7；size 代码 172 / 1000、文档 6 / 1500；disclosure、`git diff --check` 干净。

## 上一轮 6 条复核（全部在当前 head 用变异证伪）

P1「重试不得产生第二份工作树」：注入 stray 工作树 → 红；「总数不变但复用错了工作树」→ 句柄断言红。P2 恢复用例：恢复失败 → `failed` 红、同刻租约 → `provisioning` 红、源码变异写出第二条关系 → `2 !== 1` 红。P2 句柄规范化、三条 P3 均已修。

## 本轮意见

| 级别 | 位置 | 意见 |
|---|---|---|
| P2 | `local-git-core-provisioning.test.js:90` | 「core 不得报 ready」只在新上下文的供应路径上成立；已 ready 的上下文在工作树 / 分支被删后再 Start Work 仍报 `ready / saved / confirmed`（`main` 既有，已登记 #211）；用例 2 经 `startWork` 没有回读磁盘 |
| P3（PR 级） | 共享 ExecPlan | 本 PR 自己这一轮的处置没有写回共享 ExecPlan 的 Decision Log；描述里的「第五轮」与共享 ExecPlan 里已用掉的「第五轮」撞号 |

## #207 验收与关联裁决

1 部分（经 `provisionGit` 断言了磁盘，经 `startWork` 的用例没有）；2 成立；3 成立；4 部分（供应路径成立，已 ready 上下文不成立，见 #211）；5 成立。两位子评审在 `Refs` / `Closes` 上结论相反，主评审裁决**保持 `Refs #207`**：验收 4 的字面在已 ready 上下文上有可复现反例（主评审在 `b0e8499`、`329a475` 与 `main` 替身上各复现一次）。

## 收口顺序

#160 修复并合并后：`git rebase --onto origin/main <#160 合并前的 head>`，重跑本文件与三层测试。

## 第六轮响应后的锁定事实（2026-09-26）

三条（1 条 inline P2、2 条并入 P2 的磁盘回读、1 条 PR 级 P3）**全部判为属实**并已处置，逐条改法与判别性
证据见共享 ExecPlan 的「评审响应（2026-09-26，第六轮：#209 的 2 条）」。要点：

- 「core 不得报 ready」的用例名与 README 行收窄为「**供应路径上**不得报 ready」，并就地写明已 ready 上下文
  的反例与 **#211**；`Refs #207` 保持（裁决理由与本记录「#207 验收与关联裁决」一致）。
- 「恢复供应」用例补三条**磁盘**断言（#207 验收 1 的「exist on disk」）；变异（期望路径改错）→ `5 / 1`。
- PR 级 P3：本轮处置写回共享 ExecPlan，并新增「栈级轮次编号（唯一口径）」——四个 PR 共用一个 ExecPlan，
  轮次按栈编号，PR 描述不再自数。

**本记录锁定的 head `91bd14d` 已不存在**：`#208` 合入 `main` 后，本分支用
`git rebase --onto <#160 新 head> b0e8499` 收口（锚点 `refs/backup/20260926-161556/test/local-git-core-provisioning`）。
上面「锁定事实」一节的数字因此全部作废，保留为评审当时的证据。
