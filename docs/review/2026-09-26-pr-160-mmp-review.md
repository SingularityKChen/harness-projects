# PR #160 Start Work 栈第六轮 MMP 交付评审记录

> 状态：Blocked（1 × P1）→ **P1 与 5 条 P2/P3 已在第六轮响应中处置**（见文末「第六轮响应后的锁定事实」；
> 评审原文保留为历史）。
> 本记录由评审者写在作者的工作区、未提交；作者修复时随修复一并提交。
> 评审日期：2026-09-26（Asia/Shanghai）
> PR：[#160](https://github.com/SingularityKChen/harness-projects/pull/160)，声明 Closes #137
> 关联 ExecPlan：`docs/exec-plan/active/2026-09-24-local-git-worktree.md`
> 跨 PR 矩阵与并集预演：`docs/review/2026-09-26-start-work-stack-round6-review.md`（随 #208 进入 `main`）

## 锁定事实

- 评审 head：`b0e8499`（3 个提交），base = #208 评审时 head `7490197`；review id `5325081285`，6 条 inline（1 × P1、2 × P2、3 × P3）。
- 评审 head 上的实测：`tsc --noEmit` exit 0；`node --test tests/contract tests/integration tests/e2e` → 638 / 638；mvp0 7 / 7；boundaries 7 / 7；size 代码 **1000 / 1000**、文档 903 / 1500；三个提交各自单独运行 615 / 638 / 638。
- #208 已在本轮修复后以三个新提交合入 `main`（原 base `7490197` 不再存在）：本 PR 需要 `git rebase --onto origin/main 7490197` 收口，再重跑上面的命令。#208 的套件修复打到本层 head 上实测 638 / 638、本地 Git 适配器 23 / 23。

## 第四轮 18 条复核

17 条已修且变异有牙（默认分支推断链、`createContext` 夹具、`pack-refs` 大小写变体、写后回读、两个守卫、argv 扫描 8 种绕过、`removeWorktree` 移出、`trim()`、`..wi`、`lstrip=2`、提交卫生、验收 3 措辞）。**P1①部分修复**：逃出仓库的符号链接已拒，指向仓库内部的被跟踪 `.worktrees` 链接仍被跟随。

## P1（主评审独立复现）

上游仓库提交 `.worktrees -> <目标>` 与被跟踪的 `src/keep.txt`，受害者 `git clone` 后按默认配置 `composeCore` + `commands.startWork`（不注入 `allowedRoot`）：

| 目标 | base `7490197` | head `b0e8499` / 栈顶 `329a475`（逐字相同） |
|---|---|---|
| `src` | N/A（占位包） | `ready / saved / confirmed`，工作树在 `src/wi-1`；主检出 `git add -A` 暂存 `A src/wi-1`（嵌入仓库，绕过 `info/exclude`） |
| `.git` | N/A | `ready / saved / confirmed`，检出落在 `.git/wi-1` |
| `../outside`（不存在） | N/A | `failed`：「路径分量是悬空符号链接」 |

违反 `AGENTS.md` §7「未经允许的符号链接」；#137 验收 1（「one reached through a symlink … refused before any Git command runs」）在默认配置下字面不成立。

**重新入队条件**：派生默认根上的 `<repo>/.worktrees` 若是符号链接，在任何 Git 调用之前结构化拒绝（零调用断言）；补一条被跟踪 `.worktrees -> src` 的判别用例（去掉判定后必须变红）；宿主注入的允许根按同一判定收口或登记 issue；在 1000 / 1000 的体量下回收等量行数或再拆一层。

## P2 / P3（inline）

| 级别 | 位置 | 意见 |
|---|---|---|
| P2 | `tests/integration/README.md:61` | 栈底写上层事实：登记了只在 #209 才存在的 `local-git-core-provisioning.test.js` |
| P2 | ExecPlan `:335` | Purpose 与验收表仍把 `removeWorktree` 写成本层交付物 |
| P3 | ExecPlan `:367` | 实测数字与验收表未回填 |
| P3 | `provider.ts:176` | `fromRef` 无前置校验、无 `--end-of-options`（实测不可利用，防御纵深） |
| P3 | `provider.ts:178` | 「起点 main不存在」里的 `main` 是 core 兜底编出来的 |

## #137 验收（字面）

1 部分（见 P1）；2 成立（经 core 的证据在 #209）；3 成立；4 成立；5 成立。

## 第六轮响应后的锁定事实（2026-09-26）

六条意见**全部判为属实**（每条在隔离 worktree 里由独立 agent 自写脚本、跑真实 git 后判定，不采信本记录
给出的输出）；其中 [6] 的**修法归属**被驳回——根在 core 的 `baseRef()` 的 `?? 'main'`，见 ExecPlan 的
「系统级根因分析」C 与 `Decision Log` D30（**需人类确认**）。逐条处置、判别性证据与体量读数见 ExecPlan 的
「评审响应（2026-09-26，第六轮）」；P1 的判据换成不变量（检出内的分量不得是符号链接），`rootWithin` 与它的
归属比较随之删除。易失值一律回读（`git rev-parse HEAD`、`node scripts/rule-checks.mjs size origin/main`），
不在本记录里写死。

**本记录锁定的 head `b0e8499` 已不存在**：#208 以三个新提交合入 `main` 后，本分支用
`git rebase --onto 9b33fe5 7490197` 收口（锚点 `refs/backup/20260926-161556/feat/local-git-worktree`）。
上面「锁定事实」一节的数字因此全部作废，保留为评审当时的证据。
