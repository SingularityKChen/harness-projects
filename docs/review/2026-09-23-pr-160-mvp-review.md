# PR #160 第二轮 MMP 交付评审记录

> 状态：第二轮 Blocked（2 × P1）；P1 与 P2/P3 已在第三轮响应修复（见文末「第三轮响应后的锁定事实」），
> 第三轮复核结论见 `docs/review/2026-09-24-start-work-batch-review.md`（已随 #185 进入 `main`）。本行以下的第二轮原文保留为历史。
> 评审日期：2026-09-23（Asia/Shanghai）
> PR：[#160](https://github.com/SingularityKChen/harness-projects/pull/160)，声明 Closes #137，Refs #136、#165
> 关联 ExecPlan：`docs/exec-plan/active/2026-09-24-local-git-worktree.md`
> 跨 PR 矩阵与并集预演：`docs/review/2026-09-23-mvp1-batch-review.md`（已随 #159 合入 `main`）
> 本记录由评审者写在 `.worktrees/w11-d2a`，未提交；作者修复 P1 时随修复一并提交，并按新 head 更新"锁定事实"。

## 锁定事实

- 评审 head：`1830136fd8d0`（19 个提交），base `main@940046ec15c3`；review id `5291826404`，13 条 inline。
- 评审 head 上的实测（在检出 `feat/local-git-worktree` 的 `.worktrees/w11-d2a` 根目录运行）：`node scripts/rule-checks.mjs size origin/main` → 代码 988 / 1000、文档 490 / 1500；`pnpm verify` → 468 / 468 + 7 / 7；`node --test tests/integration` → 25 / 25；三个聚焦文件 → 35 / 35。
- 并集（#159 → #160 → #161 → #158）`pnpm verify` 532 / 532 + 7 / 7，并集层无 P0。

## P1（阻塞）与独立复现

1. **`createWorktree` 对不可复用的状态报 `conflict`，core 据此报假 `Ready` / `confirmed`**（`packages/providers/development-local-git/src/provider.ts:188-190`、`:226`；消费方 `packages/core/src/git-provisioning.ts` 的 `ensureWorktree` 对 `Conflict` 不探测就 `confirmWrite`）。真实 `provisionGit` + 本 provider、临时仓库，五个反例全部复现：

   | 构造 | provider | `provisionGit` | 磁盘上的事实 |
   |---|---|---|---|
   | 主检出上执行过 `git checkout -b work/wi-1` | `conflict`（`already used by worktree`） | `ready` / `confirmed` | 工作树路径不存在 |
   | 同一分支换新路径重试 | `conflict` | `ready` / `confirmed` | 新路径不存在 |
   | 路径已登记但检出的是 `other` | `conflict` | `ready`，`branchExternalId: work/wi-1` | 实际检出 `other` |
   | 已登记，目录被换成普通目录 | `conflict` | `ready` / `confirmed` | 目录内 `git rev-parse --show-toplevel` 返回主仓库 |
   | 叶子为悬空符号链接 | `conflict`（`already exists`） | `ready` / `confirmed` | 没有工作树 |

2. **#137 验收 3 在 core 层不成立**（`provider.ts:163-166`；`git-provisioning.ts` 的 `ensureBranch` 对 `Conflict` 做 `branchProbe` 后复用）：`work/wi-1` 预先指向无关 orphan 提交时，provider 报 `conflict`，`provisionGit` 返回 `ready` / `confirmed`，工作树 HEAD 是那个无关提交。

## #137 验收逐条

| # | 判定 |
|---|---|
| 1 越界 / `..` / 符号链接先于 Git 被拒 | 满足（大小写变体、NFC / NFD、前缀陷阱、尾随斜杠、允许根 = 仓库均正确） |
| 2 重试返回已有工作树与分支 | 部分（身份不同，#165；P1-1 情形下对不存在的工作树报 Ready） |
| 3 同名分支只在指向请求位置时复用 | 端到端不满足（P1-2） |
| 4 Git 只经 argv | 满足（选项形态的 ref 在 `rev-parse --verify` 失败为 `not_found`；建议补 `--end-of-options`） |
| 5 契约套件通过 | 满足，有两处子集假绿空间（P2） |

## P2 / P3

逐条见 GitHub review `5291826404` 的 inline 意见：裸异常（`paths.ts:74`，#174 的前提因此有误）；「目录已消失」无用例；`provides` 自报；`objects` 钩子从不被调用；#165 表征收口指令误导；证据数字矛盾与 PR 描述占位符；提交卫生（11 个提交缺 `Refs`、4 个正文为空、"压缩 × 4 → 恢复"的来回）；defaultBranch 回退、stderr 首行、分类顺序、短 sha 与分支同名、换行路径、默认允许根；分支前缀 `feat/`。

## 重新进入排队的条件

- 两条 P1 修复，并各补一条经 `provisionGit` 断言"不报 `ready`"的用例；代码预算只剩 12 行，需先做减法或拆批次。
- 修复后重新锁定 head、按 `docs/project-management/merge-queue.md` §2.2 对当前 `main` 重做预演，并复跑上表五个反例与分支指向别处的反例。
- #161 叠在本 PR 上，本 PR 合并前 #161 不可合并。

## 第三轮响应后的锁定事实（2026-09-24）

第二轮的两条 P1 与 13 条 P2/P3 的处置与判别性证据见 ExecPlan 的「评审响应（2026-09-23，第二轮）」与
「评审响应（2026-09-24，第三轮）」；本记录的「重新进入排队的条件」已全部满足：

- 两条 P1 修复并各补经 `provisionGit` 断言「不报 `ready`」的用例（默认允许根、四个 lookalike）；
- 恢复用例改成断言 `has_worktree` 关系数 = 1（#185 已修 core）；
- 已对含 #185 的 `main` 重做预演并复跑五个反例与「分支指向别处」的反例。

在包含本行的 head 上实测（回读：`git rev-parse HEAD` 加同一条命令；易失值不写死）：
`pnpm verify` 536/536 + mvp0 7/7、`pnpm run boundaries` 7/7、`node --test tests/integration` 45/45、
`node --test tests/contract/development-contract.test.js` 13/13、三个聚焦文件 37/37；
`size origin/main` 代码 **1121 / 1000**（超出 121，原因见 ExecPlan `Outcomes` 的「体量超出」）、文档 721 / 1500；
`disclosure` 与 `git diff --check` 均通过。

> **Superseded by 第四轮（2026-09-26）**：上面这组数字属于当时的 head，第四轮修复后已不再成立（现行数字见 ExecPlan
> 的「评审响应（2026-09-26，第四轮）」与 PR 描述）。原文保留为历史。

## 第四轮响应后的锁定事实（2026-09-26）

第四轮 review（2026-09-25T16:17）在 #160 留下 18 条未解决 inline 意见（2×P1、6×P2、10×P3），逐条处置见 ExecPlan 的
「评审响应（2026-09-26，第四轮）」；两条被判为「部分属实」并写明成立条件（`coreContextFor` 的装配顺序只在 SQLite
业务表落地后才复现；默认根未被忽略对本仓库不成立、对消费仓库成立）。

本轮同时重整了交付边界：套件改动拆到 **#205**（基线 `main`）、工作树移除交 **#138**、core 供应序列验收拆到 **#207**。
`Closes #137` 的字面前提由人类伙伴 2026-09-26 批准修正 #137 验收条目 3 的措辞后成立。

易失值一律回读（`node scripts/rule-checks.mjs size <base>`、`git rev-parse HEAD`），不在本记录里写死。
