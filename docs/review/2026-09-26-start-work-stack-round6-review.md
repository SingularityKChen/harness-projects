# Start Work 栈第六轮（#208 → #160 → #209 → #161）MMP 交付评审

> 状态：Completed（评审已提交；#208 就地修复后合并，#160 因 1 × P1 不合并，#209 / #161 叠在其上不合并）
> 评审日期：2026-09-26（Asia/Shanghai）
> 范围：Start Work 栈四个 PR（GitHub 原生 stack）：#208（套件，第二轮）、#160（本地 Git provider）、#209（core 供应序列验收，第二轮）、#161（人工执行 provider）
> 评审方式：锁定 head / base / checks / threads → 并集预演 → 按 PR 分派只读子评审 + 一个产品闭环评审 + 一个流程审计 → 阻塞级结论由主评审在 base / head / 栈顶独立复现 → 建完 P0–P3 矩阵后一次性提交四份 review → 只对无 P0 / P1 的 PR 就地修复、归档计划并 rebase merge

## Purpose / Big Picture

回答「每合并一层，MMP 的哪一段闭环真的多成立了一段」，而不是「测试是否全绿」。本栈的交付是：Development 契约套件按能力快照驱动（#205）、真实本地 Git 上出现工作树与分支（#137）、core 供应序列上的系统验收（#207）、可绑定的人工执行（#139）。**整栈是库级闭环**：非测试代码里没有任何宿主组装 `development-local-git` 或 `execution-human`（宿主组合根 #132、UI #142），合并整栈后用户可观察的变化为零；在 SQLite 上 core 的 Start Work 仍受 #187 / #188 阻塞。

## Context and Orientation

| PR | 评审时 head | 评审时 base | 关联 | review id | inline | 结论 |
|---|---|---|---|---|---|---|
| #208 | `7490197` | `main@53aadd5` | Closes #205 → 修复后 Refs #205 | `5325081139` | 10（5 × P2、5 × P3） | 无 P0 / P1 → 修复后合并 |
| #160 | `b0e8499` | #208 head | Closes #137 | `5325081285` | 6（1 × P1、2 × P2、3 × P3） | **1 × P1**，不合并 |
| #209 | `91bd14d` | #160 head | Refs #207 | `5325081532` | 1（P2）+ PR 级 P3 | 本层无 P0 / P1，受 #160 阻塞 |
| #161 | `329a475` | #209 head | Refs #139 | `5325081597` | 7（2 × P2、5 × P3） | 本层无 P0 / P1，受 #160 阻塞 |

- 分支保护：唯一必需检查 `PR Fast Gate`，`strict = true`；仓库只允许 rebase merge，`delete_branch_on_merge = true`；四个 PR 同属一个原生 stack，合并走 `merge-async`。
- 评审账号与开发账号分离：四份 review 由评审账号提交，修复与提交用开发账号。提交前重锁四个 head，与载荷的 `commit_id` 逐一一致。
- 上一轮（第五轮，只覆盖 #208 与 #209）与第四轮（#160、#161）的全部 thread 均已 resolve；本轮把每条旧意见当成待验证假设，在当前 head 上复跑。

## Design / Spec

- **并集先于单 PR**：一次性 clone 里把本栈栈顶与 SQLite 栈 #170 → #175、#181 按两种顺序变基；评审期间 #170 / #175 已合入 `main`，又在真实的 `main` 上重建了一次并集。
- **子评审分工与模型**：#208（契约语义）、#160（安全边界）、产品闭环三个用 Opus；#209、#161、流程审计三个用 Sonnet。子评审只读、只写 `comments.json`。
- **阻塞级复现**：P1 与「假成功」类结论由主评审自写脚本在 base / head / 栈顶各跑一次；复现只用良性观察（检出落在哪里、core 报什么、主检出 `git add -A` 暂存了什么），不构造任何载荷。

## 并集预演

- 预演一（SQLite 栈合并前的分支 head）：两种顺序唯一冲突是 `docs/README.md` 索引，两种顺序的代码树逐字节相同；`pnpm verify` = 747 / 747 + mvp0 7 / 7。
- 预演二（真实 `main`，含已合并的 #170 / #175）：本栈变基到 `main` 后 `pnpm verify` = 747 / 747 + 7 / 7。
- 本栈栈顶单独 `pnpm verify` = 665 / 665 + 7 / 7。并集层无 P0。

## 风险矩阵

### P0 —— 无

### P1（主评审独立复现）

| PR | 条目 | 复现 |
|---|---|---|
| #160 | 第四轮 P1①（「默认允许根自身是符号链接时不核对」）只修了一半：派生的默认允许根仍跟随**指向仓库内部**的被跟踪 `.worktrees` 符号链接 | 上游提交 `.worktrees -> src` / `.worktrees -> .git`，受害者 clone 后默认配置 Start Work：base `7490197` N/A（占位包）；head `b0e8499` 与栈顶 `329a475` 相同——`src` 形态工作树落在被跟踪目录 `src/wi-1`，主检出 `git add -A` 把它暂存为嵌入仓库（绕过 `info/exclude`）；`.git` 形态检出落在 `.git/wi-1`；两者都 `ready / saved / confirmed`。逃出仓库的形态已被拒。违反 `AGENTS.md` §7「未经允许的符号链接」，#137 验收 1 在默认配置下字面不成立 |

### 降级与裁决（理由写在对应 inline 或 PR 级意见里）

- #208「分支 / 工作树未按快照驱动」：子评审提请考虑升级。它让诚实的子集 provider **假红**、不产生假绿（谎报可用的六种适配器全红），不影响任何现存 provider → P2，留在 #205。
- #209 的 `Refs #207` 是否应改 `Closes`：两位子评审结论相反。裁决保持 `Refs`：#207 验收 4 的字面「never produces `ready`」在已 ready 的上下文上有可复现反例（见下），此时关闭会让字面不成立的验收被关掉。
- #160 README 是否「栈底写上层事实」：流程审计判不构成，另两位子评审判构成。主评审核对：`b0e8499` 的 `tests/integration/README.md` 登记了 `local-git-core-provisioning.test.js` 及其五条不变量，而该文件在这一层不存在（`git cat-file -e` 失败）→ 构成，P2。
- 流程审计的一条低置信度 P3（Bottom Change Note 条目顺序）不提交：两条记录同属一个提交，无法证明顺序错，也不影响任何断言。

### 既有缺陷（不阻塞本栈，新开 #211）

已 ready 的执行上下文在工作树被删（`rm -rf` 留下 prunable 登记，或 `git worktree remove` + `git branch -D`）后，换新幂等键再 Start Work，core 仍报 `ready / saved / confirmed`：`main@53aadd5` 纯替身、#160 head、栈顶三处结果相同；本栈对 `packages/core` 零改动（`git diff --stat 53aadd5 329a475 -- packages/core` 为空），所以是 `main` 上 core 的既有行为，登记为 #211。

### P2 / P3 计数（inline）

| PR | P1 | P2 | P3 |
|---|---|---|---|
| #208 | 0 | 5 | 5 |
| #160 | 1 | 2 | 3 |
| #209 | 0 | 1 | 0（另有 1 条 PR 级 P3） |
| #161 | 0 | 2 | 5 |

## MMP 判定

| PR | 本层合并后多成立了什么 | 说明 |
|---|---|---|
| #208 | 契约键表多了 `development.worktree.remove`；套件不再向未声明移除的 provider 索取移除；变更请求三个方法按各自的键驱动 | #205 保持开启：分支 / 工作树创建按快照驱动与本地 Git provider 通过套件（随 #160）未成立 |
| #160 | 真实本地 Git provider；手写组装下步骤 7 真的落盘 | P1 修好之前，#137 验收 1 在默认配置下不成立 |
| #209 | 只增加证据：6 条经 core 的集成用例，上一轮 6 条意见全部修复且变异有牙 | #207 验收 1、4 部分成立 |
| #161 | 可绑定的人工执行；手写组装下步骤 8 的成功形态（`running`） | #139 验收 2 只在无区分度的意义上成立，#171 / #172 如实登记 |

## Progress

- [x] (2026-09-26) 锁定四个 head / base / checks / threads；导出前几轮全部意见作复核清单。
- [x] (2026-09-26) 并集预演（两种顺序 + 真实 `main`）。
- [x] (2026-09-26) 六个子评审完成；P1 与既有缺陷由主评审独立复现；新开 #211。
- [x] (2026-09-26) 四份 review 一次性提交（24 条 inline，行号逐条对照各自 diff 的 hunk 头校验）。
- [x] (2026-09-26) #208 就地修复、归档计划、变基到 `main` 后合并（修复与合并记录见 [2026-09-26-pr-208-mmp-review](2026-09-26-pr-208-mmp-review.md)）。

## Surprises & Discoveries

- **修复「只修了一半」的形态最难看见**：第四轮 P1①的标题是「允许根自身是符号链接」，改法却只核对「解析后仍在仓库内」。逃出仓库的反例全部变红、PR 描述也如实，于是「已修」看起来成立；只有把原意见的条件（符号链接本身）逐字当成判据重跑，才看得到指向仓库内部的那一半。
- **评审期间 `main` 前进了**：#170 / #175 在评审中途合入。预演用的是它们合并前的 head；合并前的代码与已合并版本在 `packages` / `tests` 下还差 40 行，所以在真实 `main` 上重建并集并重跑了一次。
- **「子评审结论相反」要回到字面**：#207 的 `Refs` / `Closes` 与 #160 README 两处分歧，都是一方只看了本层 diff、另一方看了「只合并到这一层时 `main` 说什么」。

## Decision Log

- Decision: #160 的包含性缺口定为 P1，不按子评审建议的 P2。
  Rationale: 它是第四轮 P1①的同一根因（符号链接本身被跟随），在默认配置下可达，#137 验收 1 字面不成立；与第四轮同一威胁模型（仓库内容是不可信输入）保持一致的定级。
  Date/Author: 2026-09-26 / 主评审
- Decision: #208 的两条需要设计判断的 P2（分支 / 工作树按快照驱动、`AccessLevel` 四态）不在合并前改语义，前者留在 #205，后者取最小修（不再声称支持 `ReadOnly`）。
  Rationale: 合并前的就地修复只做机械、局部、不扩大范围的改动；这两条都要重新设计套件在「未声明」时断言什么。
  Date/Author: 2026-09-26 / 主评审

## Idempotence and Recovery

- 本记录只描述评审事实；每个数字都附命令，可在对应 head 上重跑。
- #208 修复前的 head 以本机 backup ref 保存；远端可回读的锚点是 PR #208 的提交历史。

## Interfaces and Dependencies

- 单 PR 记录：[2026-09-26-pr-208-mmp-review](2026-09-26-pr-208-mmp-review.md)（本记录随 #208 合入）；#160 / #209 / #161 的单 PR 记录写在各自的工作区，随作者修复提交。
- 上一批次记录：[2026-09-26-mmp-round5-batch-review](2026-09-26-mmp-round5-batch-review.md)（Start Work 栈第四轮）。

## Outcomes & Retrospective

- #208 合并；#160 需要：派生默认根上的符号链接一律拒绝 + 判别用例，同时在 1000 / 1000 的体量下回收等量行数。#160 合并后，#209 / #161 按 `git rebase --onto origin/main <下层旧 head>` 收口。
- 新开 #211；#205、#207、#137、#139 保持开启。

## Bottom Change Note

- 2026-09-26：创建。评审提交、#208 修复与合并后写成。
