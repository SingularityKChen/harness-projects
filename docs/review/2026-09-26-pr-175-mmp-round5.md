# MMP 交付评审（第五轮）· PR #175（栈顶）· head `253c34d` · 结论：**2 × P1，`Closes #120` 与验收 1 字面不符，本轮不合并**

> 状态：Blocked（第五轮不合并）。第六轮复核两条 P1 均已修复，结论见 `docs/review/2026-09-26-pr-175-mmp-round6.md`
> 评审日期：2026-09-26（Asia/Shanghai）
> GitHub review：`5319966172`；inline 意见在 PR 上，本记录是 PR 级结论与重新入队条件
> 本记录由评审者写在本工作树，随第六轮修复一并提交（2026-09-26）；下面的**锁定事实是第五轮评审时点的快照**（head `253c34d` 已被重写，当前 head 以 `gh pr view 175 -R SingularityKChen/harness-projects --json headRefOid` 回读）。批次记录（含全部八个 PR 的矩阵与合并结果）已随 #167 进入 `main`：`docs/review/2026-09-26-mmp-round5-batch-review.md`

**锁定事实**：head `253c34d0e149`（3 个提交），base `d84bc16`（= #170 head），`CLEAN`，12 / 12 checks 通过，`closingIssuesReferences = [120]`，另 `Refs #5`。栈顶即并集：contract 514、integration 65、e2e 39、mvp0 7，共 625 / 625；`tsc`、`boundaries` 7 / 7；`size` 代码 630 / 1000、文档 802 / 1500（描述写 794）。描述里的「本轮新 head `93644ac`」不是当前 head 的祖先，第四轮 9 条回复都指向它。

### 风险矩阵
| 级别 | 条目 |
|---|---|
| P0 | 无 |
| **P1** | ① `storage-contract.test.js:49`：SQLite 没有注册执行组与身份同步组——PR 标题「两个实现跑全部契约组」与 #120 验收 1 都不成立。把执行组临时注册到 SQLite 后 13 / 13 通过、6 个存活变异（候选不降级、升级删候选、读隔离、写尝试工作区隔离 × 2、`findActive` 状态过滤）全部被杀——实现没问题，缺的是装配 |
| **P1** | ② `storage.ts:63`：`replacePlanningProjections` 从 `atomic` 改回 `mutate`，钉住它的重启用例被删；base 的用例在 head 上 `actual: []`——事务外调用时外键失败会把既有投影整批删掉 |
| P2 | 5 条 inline：`putExecutionContext` / `putRelation` 仍走 `mutate`（描述称已改 `atomic`）；R4-4 的分叉用例不存在、替身新加的 F1–F10 检查全部存活；`docs/README.md:49` R4-3 未修；「本层不声称」第 2 条与 #196 把分叉写反了；以及上面 P1 相关的计划收敛验收 |
| P3 | 4 条 inline（「遗留 14」指错、两处从名字引用改回编号；码点注释被改回「code unit」；替身实体清理是死代码；头注释「按插入序」对执行面不成立） |

### 第四轮复核（9 条）
成立 R4-2、R4-6、R4-7（但处置被 P1 ① 推翻）、R4-8；部分 R4-1（裸 SQL 删掉了，SQLite 执行组与身份同步组的装配也一起删了）、R4-4、R4-9；**未修 R4-3、R4-5**。读隔离在 SQLite 侧从第三轮的部分覆盖退到 0 / 6。

### 验收（字面）
- **#120（`Closes`）**：验收 1 **不成立**（SQLite 不跑执行组与身份同步组）；2–6 成立（重启覆盖身份、关系、执行上下文；事务抛错后重开读不到半写行；迁移重跑 `applied=[]`；文件库上的去重与幂等；boundaries）。
- **#5（`Refs`）**：正确——Start Work 在 SQLite 上仍抛外键错误。描述里「验收 3 要求 core 能以 SQLite 为 storage」指的是 #5 的验收 3，不是 #120 的，措辞易误读（P3）。

### 栈级产品闭环
替身上 Start Work 三次都成功；SQLite 上三次都抛 `FOREIGN KEY constraint failed`，外部写入为 0，执行上下文、运行、关系、写尝试、仓库表都是 0 行；重开后条目 id 相同。#187、#188、#195、#196、#189 均 OPEN。本 PR 若按现状合并关闭 #120，#132 的 blocked-by 只剩 #126，而 core 仍不能在 SQLite 上 Start Work——挂新 blocker 需要人类批准（`AGENTS.md` §7）。

### PR 级意见
- **[P2]** 描述与 head 不符四处：head、文档行数、「两个实现跑全部三组」、「改走 `atomic`」；R4-1 / R4-3 / R4-4 / R4-5 的 thread 在当前 head 上不能算已处置。
- **[P3]** 关联行重复（「`Closes #120 · Refs #5`Closes #120 · Refs #5」）。
- **[P2，跨层]** 结算令牌在栈顶没有任何用例（删掉失效逻辑后 625 / 625 全绿），与 #167 / #170 的同类意见合并处理。

### 修复方向
从作者本地 `backup/l6-wip`（`a3c46b8`，作者本地备份，远端不可解析）/ 第四轮 head `693aba4` 取回三组装配、`atomic` 与重启用例；修完前把 `Closes #120` 改为 `Refs #120`，或修完后重新回读 `closingIssuesReferences`。

### 栈级结论（#121 → #122 → #157 → #167 → #170 → #175 共用）

- **并集预演**：一次性 clone 里两栈两种顺序（SQLite 栈 → Start Work 栈、反序）都能变基，唯一的文本冲突是 `docs/README.md` 索引（两边都保留）。并集树上 `pnpm verify` 唯一的红来自 #160 的 5 条本地 Git 集成用例：它的 fixture 没有先 `putWorkspace` 就 `registerBindings`，而 #122 起的替身（与 SQLite 外键一致）要求工作区先存在；core 自己的 `createContext` 总是先建工作区。strict 分支保护保证后合并的一方在含前者的树上重跑必需检查，所以这是**硬性的合并顺序依赖**，不是会打坏 `main` 的 P0；修法落在 #160（见该 PR inline）。栈顶单独跑：625 / 625。
- **P1 分布**：#170 × 1、#175 × 2，根因相同——第四轮之后 L5 / L6 的改写把第四轮 head 上已经存在的内容丢了：SQLite 的身份同步组与执行组装配、装配守卫、结算令牌用例、两条同步用例、`replacePlanningProjections` 的 `atomic` 与钉住它的重启用例。第四轮 head `ae39655`（L5）/ `693aba4`（L6）以及作者本地的 `backup/l6-wip`（`a3c46b8`，作者本地备份，远端不可解析）里都还有这些内容，可以从那里取回，而不是重写。
- **L1–L4 无 P0 / P1**：按人类伙伴本轮授权（无 P0 / P1 代码问题时修复 P2 / P3、归档计划、整合提交后 rebase merge），评审者就地修能机械修的 P2 / P3；会扩大范围、超出体量上限或需要人类裁决的条目登记 issue。自下而上合并，每合并一层重锁下一层的 head / base 并重跑验证。
- **L5 / L6 本轮不合并**：L4 合并后，两层按 `git rebase --onto origin/main <L4 合并前的 head> <分支>` 重放，再修 P1。
- **产品闭环**：没有宿主或 app import `storage-sqlite`，整栈合并后用户观察不到变化；core + SQLite 文件库首轮同步与替身逐项一致、重启后 id 不变；Start Work 在 SQLite 上抛裸 `FOREIGN KEY constraint failed`（#187 / #188 / #195 / #196 跟踪，把它们挂成 #132 的 blocker 需要人类批准）。没有任何文档宣称 Gate E1 已通过或数据模型 v1 已冻结。
- **全栈 PR 描述**：#122 / #157 / #170 的描述每个字符之间都插了一次 `Refs #N`（模板替换缺陷），#121 / #167 留着 `BATCH` / `REL` / `EVIDENCE` / `RISK` / `HEAD` 占位符，#175 的 head 与数字过期——`AGENTS.md` §8 要求的验证证据、风险与关联节在六个 PR 里都不可读或缺失（PR 级 P2，合并前必修）。

## 重新入队条件

1. 从作者本地 `backup/l6-wip`（`a3c46b8`，作者本地备份，远端不可解析）/ 第四轮 head `693aba4` 取回 SQLite 的执行组与身份同步组装配、`replacePlanningProjections` 的 `atomic` 与钉住它的重启用例。
2. #170 修复并重放后，按 #170 的新 head 对本分支做 `git rebase --onto`；预期冲突同 #170，外加 `storage-sqlite-port.md` 的上游路径行（L4 已改成 completed 路径）。
3. `putExecutionContext` / `putRelation` 的多语句写入走 `atomic`，或让描述不再声称已改。
4. #120 验收 1 在 SQLite 跑满三组契约之前字面不成立：修完前把 `Closes #120` 改为 `Refs #120`，修完后回读 `closingIssuesReferences`。
