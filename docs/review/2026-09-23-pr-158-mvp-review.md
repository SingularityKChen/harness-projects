# PR #158 第二轮 MMP 交付评审记录

> 状态：Completed（无 P0 / P1；就地处置后以 rebase merge 合入）
> 评审日期：2026-09-23（Asia/Shanghai）
> PR：[#158](https://github.com/SingularityKChen/harness-projects/pull/158)，Closes #128，Refs #124
> 关联 ExecPlan：[2026-09-24-ui-model-presentation](../exec-plan/completed/2026-09-24-ui-model-presentation.md)
> 跨 PR 矩阵与并集预演：[2026-09-23-mvp1-batch-review](2026-09-23-mvp1-batch-review.md)

## 锁定事实

- 评审 head：`8ed0d656ea05`（10 个提交），base `main@940046ec15c3`；评审 review id `5291829135`，2 条 inline。
- `gh pr checks 158` 中的 `matrix.check fail` 是被同 head 后续运行取代而 `cancelled` 的 Rule checks（run `35851327057`），不是缺陷。
- 评审 head 上的实测（在检出 `feat/ui-model-presentation` 的 `.worktrees/w14-d3b` 根目录运行）：`node --test tests/contract/ui-model-presentation.test.js` → 21 / 21；`pnpm verify` → 467 / 467 + 7 / 7；`pnpm run boundaries` → 7 / 7；`grep -rn "github\|react" packages/ui-model/src` → exit 1。
- 并集（#159 → #160 → #161 → #158）`pnpm verify` 532 / 532 + 7 / 7，并集层无 P0。

## issue #128 验收

| # | 验收 | 判定 | 证据 |
|---|---|---|---|
| 1 | PR-backed 条目的内容种类是 change request，且不提供 start-work | 满足 | `packages/ui-model/src/derive.ts` 的"是否提供"按内容种类分支；变异"PR-backed 行给出 `start_work`"→ 20 / 1 |
| 2 | 动作可用性只来自 capability key | 满足 | `grep -rn "github" packages/ui-model/src` exit 1；可用性只查 `ReadonlyMap<string, AccessLevel>`；对抗性 `bindingId` 的性质断言 |
| 3 | 陈旧快照给出标记陈旧且带最后更新时间的行，永不空列表 | 满足 | 变异"陈旧时返回 `[]`"→ 19 / 2 |
| 4 | boundaries 通过，不 import React 或 provider | 满足 | `pnpm run boundaries` 7 / 7；`ui-model -> client, domain`（`tests/contract/package-boundaries.test.js`） |

## 风险矩阵（本 PR）

| 级别 | 条目 | 处置 |
|---|---|---|
| P0 / P1 | 无 | — |
| P2 | `WorkspaceRead` 六个字段只有 `store` 今天有生产者，且没有任何 issue 认领组装 | #178（挂 #124）；字段 → 生产者映射写进 ExecPlan 的遗留问题 |
| P2 | PR 描述证据停在旧 head（`b9e519d`、7 个提交、文档 782） | 最终 push 后按新 head 重写 |
| P3 | 能力规则与 key 字面量是 `capabilities` 的副本 | 不构成第二权威源、不需要 ADR；删除副本的路径见 ExecPlan D20，并入 #178 验收 3 |
| P3 | Batch 8 / 9 "移出注释 → 恢复注释"的来回提交 | 按交付物收敛为三个提交 |
| P3 | 分支前缀 `feat/` 不在 `AGENTS.md` §6 的允许集合内 | 只记录：合并后分支删除、不进入 `main` |

第一轮 10 条意见在评审 head 上逐条复核，全部成立（唯一决策点 `degradationOf`、`execution_context` 无读门且与 `packages/core/src/execution-context.ts` 一致、逐 key 级别、精确 `deepEqual`、64 格差分、中文中性短语、黑名单收窄、ISO 8601 正则、`accessIndex` 一次读取只建一次）。

## 合并收尾

- 恢复锚点：`backup/w14-d3b-pre-review3`（`8ed0d65`）。
- 变基到含 #159 的 `main`（`721e42a`），零冲突：#159 的 Active 索引行已随其归档移到 Completed 表。
- 提交按可独立回滚的交付物收敛为三个：实现与测试 → ExecPlan（归档）→ 本记录；收敛前后的树逐字节一致（对照锚点做 `git diff`）。
- 合并结果以回读为准：`gh pr view 158 -R SingularityKChen/harness-projects --json state,mergedAt,mergeCommit` 与 `gh issue view 128 -R SingularityKChen/harness-projects --json state,closedByPullRequestsReferences`。
