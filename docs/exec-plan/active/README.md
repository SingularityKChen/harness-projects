# docs/exec-plan/active

进行中的 ExecPlan 存放位置，文件命名 `YYYY-MM-DD-<slug>.md`，格式见 `PLANS.md`。

- **新建**：在这里创建文件；先写 `Design / Spec` 收敛方案，再写 `Plan of Work` 拆批次。
- **归档**：全部验收通过后，把文件移到 `../completed/`（文件名不变），并更新 `docs/README.md` 的索引。

本文件只用于保持目录存在（git 不跟踪空目录）并说明用法，它本身不是计划。
