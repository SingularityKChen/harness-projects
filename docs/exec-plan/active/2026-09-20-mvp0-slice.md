# 切片栈（C）ExecPlan

> 状态：Active
> 创建：2026-09-20
> 范围：让 MVP-0 的纵向链路在无凭据、无网络的条件下跑通，并让它的进度断言从"因正确原因失败"变成"全绿"。包含 core、controller、client 三个包与测试层的组合根。
> 上游输入：`AGENTS.md`、`PLANS.md`、`docs/exec-plan/active/2026-09-20-mvp0-parallel-stacks.md`（控制计划）、`docs/exec-plan/active/2026-09-20-contract-plane.md`（本栈依赖的契约）

## Purpose / Big Picture

完成后，一条链路走完并且**每一步都可被断言**：

```text
工作区 → 规划条目 → 工作项 → 开始工作 → 执行上下文 → 分支/提交 → 变更请求 → CI
```

判定方式是 `node --test tests/mvp0` 全绿，且它在 `pnpm verify` 内。过程中规划状态始终由规划 provider 拥有：CI 失败、执行完成、合并都不会改写它；任何未被 provider 确认的写入都不会被报告为"已保存"。

## Context and Orientation

### 术语

| 词 | 意思 |
|---|---|
| 组合根 | 把 core 与具体 provider 装配起来的唯一位置。本栈把它放在测试层，因为包边界矩阵不允许生产包同时 import `core` 与 `providers/*` |
| 进度轨道 | `tests/mvp0/`，一条**故意允许失败**的端到端断言；它不进入必需门禁，直到本栈最后一个批次让它变绿 |
| 执行上下文 | 规划进入工程的显式桥：工作项 + 仓库 → 工作树 + 分支 + 可选执行会话 |
| 状态策略 | `source_managed` / `harness_managed` / `manual`；它决定工程事件能否影响规划状态，默认不能 |
| 降级 | 某个 provider 不可用时，链路只在局部失败：读侧给最后已知值，写侧被拒绝并说明恢复动作 |

### 当前事实（本栈开始前）

- `packages/core`、`packages/controller`、`packages/client` 都只有 `packageId` 常量。
- 契约栈（A）已提供：领域模型、五域 port、离线替身（含 Storage 内存替身）、契约套件。
- 迁移运行器（B1）已存在，但**本栈不使用它**：SQLite 业务表属 #27/#28，受 Gate E1 约束。
- `package.json` 的 `test` 脚本运行 `tests/contract tests/integration tests/e2e`；`tests/mvp0` 由新增的 `test:mvp0` 脚本单独运行。

### 为什么先有一条会失败的断言

issue #42 的判据是"用例存在，且失败信息指向哪个节点还没有实现"，并明确禁止 skip（skip 不产生压力）。本栈把它落成 `tests/mvp0/` 轨道：每个节点一条断言，未实现时失败信息点名节点；每完成一个批次，失败断言数单调下降。批次 C5 让它全绿并把它提升进 `pnpm verify`。

## Design / Spec

### D1. core 只认识 port，不认识 provider

`packages/core` 的依赖只有 `capabilities` 与 `domain`。所有 provider 通过构造函数注入；core 内部没有任何 `if (provider === 'github')` 形态的分支，只有 capability key。组合发生在测试层。

### D2. 观察是唯一入口

外部世界的变化只有一条路径进入系统：

```text
ProviderObservation → 去重 → 顺序守卫 → 事务内 upsert → 关系更新 → 投影重算 → 修订号 +1
```

去重键由 provider 计算并被契约套件固定；顺序守卫比较来源版本，旧观察不得覆盖新观察；重复观察不产生第二个实体——这条是本栈最高优先级的断言（`tests/README.md` §2 第 1、4 类）。

### D3. 写路径的状态机

```text
validating → writing → confirmed | reconciled | unknown | conflict | failed
```

- `confirmed` 需要 provider 确认或 read-after-write 证据；在这之前，调用方只能看到 `saving`。
- `unknown`（例如创建结果在传输中丢失）必须先 reconcile，禁止盲目重试创建。
- `conflict` 保留最后已知权威值与本次尝试值，并提供重新应用与刷新两种恢复动作。
- 写尝试以 `(workspace, idempotency_key)` 去重：同键重放返回原结果。

### D4. Start Work 是补偿序列，不是一次调用

固定顺序：写 pending 执行上下文 → 创建工作树与分支 → 启动执行。每一步都在提交后才做下一步：

| 失败点 | 必须观察到的结果 |
|---|---|
| 工作项或仓库不合法 | 不产生任何执行上下文 |
| 工作树创建失败 | 执行上下文标记失败，**不启动**执行 |
| 执行启动失败 | 执行上下文保留并降级为 `manual_fallback`，工作树与分支仍在 |
| 同一工作项重复开始 | 返回已存在的 active 上下文，不产生第二份工作树或分支 |
| 工作树是脏的 | 不自动清理；清理只在显式请求时进行，且脏工作树必须被拒绝而不是被覆盖 |

### D5. 投影只承载四类内容

查询只返回：权威快照、外部事实的缓存、派生投影、能力与新鲜度元数据。普通查询**不得**隐式触发外部写入。派生值（Attention、Ready to Merge 等）必须带 `derived` 标记，且永不写回外部字段。

### D6. 状态策略与不变量 3

`source_managed` / `harness_managed` / `manual` 三种策略都不得让"CI 失败""执行完成""PR 合并"直接改写规划状态。唯一允许的联动是显式配置的策略加上显式的状态写入命令。这条要在三种策略下各有一条负向测试。

### D7. 被放弃的方案

| 方案 | 为什么放弃 |
|---|---|
| core 内直接 new 出离线 provider 以便跑通 | 会让生产包依赖实现，边界测试会拦下；而且真实 provider 替换时要把装配逻辑从 core 里再剥出来 |
| 让 controller 暴露 provider 原生对象 | 前端会因此依赖平台形状；wire 层只传内部对象 |
| 用 SQLite 落执行上下文 | #27/#28 受 Gate E1 约束；本栈用 Storage 契约的内存替身，验证的是"控制事实被持久化到契约边界"，不是"表长什么样" |
| 进度轨道直接进必需门禁 | 分支保护要求 `PR Fast Gate` 绿；故意失败的断言必须与必需门禁分开，否则红叉会常态化 |
| 一次 PR 交付整个 core | 超过单 PR 体量上限，且失败时无法定位到具体能力 |

## Global Constraints

- 不引入任何运行时依赖（含 uuid 库：用 `node:crypto`）。
- 单文件 ≤ 200 行，单函数 ≤ 40 行。
- core / controller / client 不得 import 任何 provider 或 storage 实现；边界测试必须保持绿。
- `client` 不得 import React，也不得访问外部平台。
- 所有断言在无凭据、无网络条件下通过。
- 只允许 rebase merge；不自行合并 PR。

## Plan of Work

### Batch C1 · MVP-0 进度断言（Closes #42）

**最小闭环**：链路变成一条会失败的断言，失败信息点名未实现的节点；它不污染必需门禁。

**涉及文件**：`tests/mvp0/chain.test.js`、`tests/mvp0/README.md`、`package.json`（新增 `test:mvp0` 脚本）、`tests/README.md`（登记进度轨道）

**验证**：

```bash
node --test tests/mvp0     # 期望：失败；每条失败信息点名未实现的节点
pnpm verify                # 期望：全绿（进度轨道不在其中）
```

**回滚**：`git revert`；删除进度轨道与脚本即可，其它栈不受影响。

### Batch C2 · core 引导与投影（Closes #76）

**最小闭环**：从观察到稳定事实——创建 Workspace、解析外部身份、引导条目、回答投影查询；重复观察不产生重复实体；provider 离线时降级。

**涉及文件**：`packages/core/src/**`、`tests/e2e/chain-bootstrap.test.js`

**验证**：

```bash
node --test tests/e2e
node --test tests/mvp0     # 期望：失败断言数少于 C1
pnpm run boundaries
```

**回滚**：`git revert`；A 栈与 F1 不受影响。

### Batch C3 · Start Work 与执行上下文（Closes #77）

**最小闭环**：写路径状态机 + Start Work 补偿序列 + 重启后可恢复。

**涉及文件**：`packages/core/src/**`、`tests/e2e/start-work.test.js`、`tests/e2e/write-machine.test.js`

**验证**：

```bash
node --test tests/e2e
node --test tests/mvp0
```

**回滚**：`git revert`；core 的引导批次仍自洽。

### Batch C4 · 交付谱系与状态策略（Closes #78）

**最小闭环**：工作项 → 执行上下文 → 分支 → 提交 → 变更请求 → CI 的谱系成为可查询投影；候选关系与已确认关系分离；三种状态策略下工程事件都不改写规划状态；缺能力报"不可用"。

**涉及文件**：`packages/core/src/**`、`tests/e2e/delivery-lineage.test.js`、`tests/e2e/status-policy.test.js`

**验证**：

```bash
node --test tests/e2e
node --test tests/mvp0
```

**回滚**：`git revert`。

### Batch C5 · Controller、Client 与门禁提升（Closes #79 #7）

**最小闭环**：core 能力通过类型化 API 与 React-free 客户端模型可达；增量订阅能发现缺口并重拉基线；MVP-0 断言变绿并被提升进 `pnpm verify`。

**涉及文件**：`packages/controller/src/**`、`packages/client/src/**`、`tests/e2e/client-sync.test.js`、`tests/e2e/controller-roundtrip.test.js`、`package.json`（把 `test:mvp0` 加入 `verify`）

**验证**：

```bash
pnpm verify
node --test tests/mvp0
pnpm run boundaries
```

**回滚**：`git revert`；从 `verify` 中移除 `test:mvp0` 即回到 C4 状态。

## Validation and Acceptance

| # | 验收项 | 判定证据 | 结果 |
|---|---|---|---|
| 1 | 链路在无凭据无网络下跑通 | `node --test tests/mvp0` 在 C5 的 head 上全绿 | 待验证 |
| 2 | 失败数单调下降 | C1→C5 每个 head 上的失败断言条数记录在本文件 `Progress` | 待验证 |
| 3 | 规划与工程正交 | 三种状态策略各一条负向用例 | 待验证 |
| 4 | 未确认不显示为已保存 | 写路径用例断言 `confirmed` 之前的状态与冲突时的双值 | 待验证 |
| 5 | 单 provider 离线只局部失败 | 故障注入用例：链路其余部分仍可查询 | 待验证 |
| 6 | 重启后执行上下文可恢复 | 用同一份 Storage 内容新建 core 后按工作项与仓库查到上下文 | 待验证 |
| 7 | 重复 Start Work 不产生第二份工作树 | 幂等用例 | 待验证 |
| 8 | client 无 React、无 provider | `pnpm run boundaries` | 待验证 |

## Progress

- [x] (2026-09-20) Batch C1 · MVP-0 进度断言（#42）：基线 head 上七条断言全部失败且全部点名未实现的节点（`node --test tests/mvp0` → `tests 7 / pass 0 / fail 7`）；契约+集成 250/250 仍全绿
- [x] (2026-09-20) Batch C2 · core 引导与投影（#76）：`node --test tests/e2e` → `pass 7 / fail 0`；`node --test tests/mvp0` → `pass 3 / fail 4`，失败数 7 → 4，节点 1–3 通过、4–7 仍点名未实现的 `commands.startWork` / `queries.getExecutionContext` / `queries.getDeliveryLineage`；`tsc --noEmit`、`boundaries`、`rule-checks size` 通过
- [x] (2026-09-20) Batch C3 · Start Work 与执行上下文（#77）：`node --test tests/e2e` → `pass 15 / fail 0`；`node --test tests/mvp0` → `pass 5 / fail 2`，失败数 4 → 2，节点 4、5 通过、6–7 仍点名未实现的 `queries.getDeliveryLineage`；`node --test tests/contract tests/integration` → 250/250；`tsc --noEmit` 通过；`rule-checks size feat/core-bootstrap` → 代码 830 / 1000 行。交付 `src/write-machine.ts`、`src/start-work.ts`、`src/execution-context.ts`、`src/git-provisioning.ts` 与 `tests/e2e/{start-work,write-machine}.test.js`
- [x] (2026-09-20) Batch C4 · 交付谱系与状态策略（#78）：`node --test tests/mvp0` → `pass 7 / fail 0`（入口 pass 5 / fail 2，节点 6、7 转绿）；`node --test tests/e2e` → `pass 22 / fail 0`（新增 `delivery-lineage` 4 条、`status-policy` 3 条）；`node --test tests/contract tests/integration` → 250/250；`tsc --noEmit`、`rule-checks disclosure`、`git diff --check` 通过；`rule-checks size feat/core-start-work` → 代码 945 / 1000 行（文档 18 / 1500）。交付 `src/relations.ts`、`src/chain-facts.ts`、`src/delivery.ts`、`src/status-policy.ts`，并扩展 `projection.ts` / `queries.ts` / `context.ts` / `start-work.ts`
- [x] (2026-09-20) Batch C5 · Controller/Client 与门禁提升（#79 #7）：`node --test tests/e2e` → `pass 31 / fail 0`（新增 `controller-roundtrip` 5 条、`client-sync` 4 条）；`node --test tests/mvp0` → `pass 7 / fail 0`；`node --test tests/contract tests/integration` → 250/250；`tsc --noEmit`、`node --test tests/contract/package-boundaries.test.js` → 7/7、`rule-checks size feat/core-delivery-lineage` → 代码 895 / 1000 行（文档 18 / 1500）通过；`verify` 已加 `test:mvp0`。交付 `packages/controller/src/{wire,queries,commands,watch,index}.ts` 与 `packages/client/src/{store,transport,sync,index}.ts`
## Surprises & Discoveries

- **Observation**：`.github/workflows/ci.yml` 的触发条件是 `pull_request: branches: [main]`，因此**栈内 PR（base 不是 `main`）不会跑 `PR Fast Gate`**——本轮只有 #80 拿到了真实 CI 证据，其余八个只有 advisory 的 `Issue policy`。
  **Evidence**：`gh pr checks <n>` 在 #80 上返回 Disclosure scan / Issue policy / PR Fast Gate 三个检查，在其余八个上只返回 Issue policy。
  **Decision impact**：栈内批次必须自带本地等价证据（`tsc --noEmit` + 完整 `node --test`），并在合并前逐分支复跑；这一条已写进 `docs/project-management/merge-queue.md` §4.2。把 CI 触发分支扩展到 `**` 是另一个批次，需要同步 `docs/development/ci.yml` 与 `scripts/workflow-check.mjs`。

- **Observation**：base 不是 `main` 的 PR 不会生成 `closingIssuesReferences`，即使描述里写了 `Closes #N`。
  **Evidence**：`gh pr view 81 --json closingIssuesReferences` → `[]`；`gh pr view 80 …` → `[74]`。
  **Decision impact**：栈内 PR 的 issue 关联靠描述与队列文档核对；GitHub 会在下层分支合并后被删除时把上层 PR 的 base 重定向到 `main`，届时 closing 关联才生效——此事必须在下一次合并后回读确认。

（实现期间如实记录。实现者必须把"哪条断言在哪一层失败"与"为什么失败"分开写，避免用"调整断言"掩盖真实缺陷。）

- **Observation**：`tests/mvp0/chain.test.js` 的 `needMethod` 对方法路径的**每一段**都要求 `typeof === 'function'`，中间命名空间对象会被判成"还不是可调用的方法"。因此 `CoreApi.queries` / `CoreApi.commands` 必须做成"可调用命名空间"（本身是函数、同时带方法属性），否则节点 1 无论 core 怎么写都不可能通过。
  **Evidence**：最小复现 `{ queries: { listProviderBindings(){} } }` → `typeof api.queries === 'object'`，`needMethod` 在第一段即 `notImplemented`；C2 第一次实现（普通对象）实测节点 1–3 全部失败于此。
  **Decision impact**：这是那条冻结断言对 CoreApi 形状的硬约束，不是实现选择；已写入 `packages/core/src/context.ts` 的 `CoreNamespace` 注释，改动前必须同步该断言。

- **Observation**：节点 3 不调用 `commands.bootstrapWorkspace` 就直接读 `queries.listPlanningItems()`，所以"组合后即有投影"必须在 `composeCore` 内完成一次首轮水合；把水合交给命令会让节点 3 只能因"没有条目"失败。
  **Evidence**：`chain.test.js:88-91` 只 `composeFor` + `listPlanningItems`；实测把水合放在查询里同样可行，但会让普通查询隐式触发同步，故选择组合时水合，查询保持纯本地只读（D5）。

- **Observation**：provider 离线时 `reconcile` 不产出任何观察（这符合契约："没有新事实"不是错误），因此**首次**引导在离线provider 上只能得到 `not_found`，而不是 `unavailable`；只有已解析过 `projectRef` 的实例才会在下次同步时把 offline 如实翻译成 `unavailable`。
  **Evidence**：降级用例必须先在线引导一次再打开 `FaultKind.Offline`，此时 `bootstrapWorkspace` 返回 `error.code === 'unavailable'`，游标 `degraded`，查询仍返回最后已知投影。

- **Observation**：`ProviderErrorCode` 不在 `@harness-projects/capabilities` 的导出面上（该包只 re-export `ProjectErrorCode`），core 需要它时只能从 `@harness-projects/domain` 取。
  **Evidence**：`tsc --noEmit` 报 `TS2724: '"@harness-projects/capabilities"' has no exported member named 'ProviderErrorCode'`。



- **Observation**：节点 4/5 用字面量占位 id（`<work-item>` / `<repository>`）调用 `commands.startWork`，且节点 5 要求两次调用返回同一个 `executionContextId`、`queries.getExecutionContext` 能查回上下文。因此 startWork 的"工作项/仓库不合法"只能是**形状校验**（非空字符串），不能是存储/registry 存在性校验：占位仓库在 development 替身里必然 `not_found`，存在性校验会让节点 5 在第一句就无上下文可查。
  **Evidence**：`chain.test.js:103,117-123`；实测把校验改成存在性校验后节点 5 在 `context != null` 处失败。
  **Decision impact**：工作项/仓库的存在性解析留给后续显式命令；这也解释了为什么"不合法不产生上下文"的负向用例用空串而不是不存在的 id。

- **Observation**：节点 5 的占位仓库必然让 git 步骤失败，所以"重复开始复用"不能只复用 storage 的 active 上下文（fake Storage 把 `failed` 视为非 active）。本批次把执行上下文 id 由 `(workspace, workItemId, repositoryId)` 哈希确定，并把该 pair 的既有上下文（含 failed）整体复用：幂等性因此是标识本身的性质，也让"重启后按工作项 + 仓库查询"退化成一次纯查询。
  **Evidence**：`packages/core/src/execution-context.ts` 的 `contextIdFor`；`chain.test.js:123` 要求 `first.executionContextId === second.executionContextId`。
  **Decision impact**：同一 pair 一旦 failed 便不再自动重开（不产生第二份工作树/分支）；显式的重试/关闭命令与"Closed 后开新周期"留给 C4+。

- **Observation**：领域枚举 `ExecutionContextStatus` 没有 `manual_fallback`，冻结的 `ExecutionContextRecord` 也没有承载降级的字段（本批次不改 contracts）。执行启动失败时把降级落成"一条 `Failed` 的 `ExecutionRun` 记录 + 结果里的 `fallback: 'manual_fallback'`"：上下文状态保持 `ready`（环境确实就绪），重启后 `getExecutionContext` 由那条运行记录仍能报出 `fallback` 与 `degraded`。
  **Evidence**：`tests/e2e/start-work.test.js` 的执行失败用例在重启/重查后断言 `view.fallback === 'manual_fallback'`；`ExecutionRunStatus.Failed` 与 `WriteState.Saved` 在此并不矛盾——git 写入已拿到 ack，失败的是那次运行。



- **Observation**：`tests/mvp0` 节点 6/7 在**没有 startWork** 的组合上调用 `queries.getDeliveryLineage({ workItemId: '<work-item>', repositoryId: '<repository>' })`，而 core 没有任何途径枚举仓库：development port 没有 listRepositories，delivery 的 listPipelineRuns 必须带仓库引用。因此"只读 provider 事实"不可能让节点 6/7 看到 `has_worktree` / `produced_by` / `runs_on`。
  **Evidence**：`tests/mvp0/chain.test.js:126-148`（占位 id、不调 startWork）；`packages/capabilities/src/development-provider.ts` 无 listRepositories；把投影先写成"只读已观察事实"后节点 6/7 实测失败于缺少跳。
  **Decision impact**：投影在真实事实之外保留"链骨架"跳：端点由 `chainEntityId`（工作空间 + 种类 + binding + 外部 id 哈希）确定，标 `observed: false`、provenance `chain_skeleton`、关系状态 `candidate`。骨架让"缺哪一跳"本身可查询，也让节点 6/7 在没有任何 provider 事实时仍可判定；它不把推断伪装成事实——`observed` 与 `relationState` 必须由调用方检查。

- **Observation**：交付方看到的仓库/变更请求是它自己 binding 下的外部对象，而 core 没有跨 provider 身份映射（Gate E1 未通过）。MVP-0 的显式假设是"同一外部 id 在不同 provider 下视为同一对象"，交付读取因此按提供该能力的 binding 各自构造引用。
  **Evidence**：`packages/core/src/chain-facts.ts` 的 `refFor`；离线替身的 delivery 种子与 development 用同一 externalId（`repo-alpha` / `pr-1`）。
  **Decision impact**：Gate E1 通过后这条假设必须由身份表替换，在那之前不得据此冻结本地数据模型。

- **Observation**：ExecPlan D6 用 `source_managed` / `harness_managed` / `manual` 命名三种状态策略，而 domain 冻结的枚举是 `provider_authoritative` / `host_authoritative` / `manual_only`（被契约测试逐字钉死）。两者指同一件事，只是两套名字。
  **Evidence**：`packages/domain/src/status.ts` 的 `StatusPolicy` 与 `tests/contract/domain-enums.test.js`。
  **Decision impact**：映射只在 `status-policy.ts` 的 `POLICY_BY_MODE` 出现一次，产品面说法与领域枚举不会再各说各话。

- **Observation**：`queries.getDeliveryLineage` 读取时会把确定性发现的关系落成候选边（三元组幂等）。这是本地控制事实写入，不是外部写入：D5 禁止的是普通查询隐式触发外部写入，provider 侧只被读。
  **Evidence**：`packages/core/src/delivery.ts` 里的 `recordEdges` 调用；`tests/e2e/delivery-lineage.test.js` 断言重复读取不产生第二个三元组、候选在显式确认前不可读成 confirmed。
  **Decision impact**：若后续要求查询绝对无副作用，应把发现过程移到显式命令；当前保留它是为了让"候选表随时可查"这条验收项成立。



- **Observation**：core 的 `CoreApi` 没有暴露 `currentRevision`，也没有变更日志；唯一的修订面是每条投影自带的 `PlanningItemView.freshness.revision`。所以 controller 的工作区修订只能取"投影修订的最大值"，而一段增量只有在订阅者游标处的快照还在手上时才能重建。
  **Evidence**：`packages/core/src/queries.ts` 的 `CoreQueries` 只有五个只读方法；`Storage.currentRevision` 只在 core 内部使用。`watch.ts` 因此在三种情况下直接发 gap：落后超过保留窗口、游标领先于工作区、以及没有订阅点快照。
  **Decision impact**：wire 契约不受影响；后续批次若把权威修订或变更日志提到 `CoreApi`，`watchWorkspace` 应从"轮询 + 取最大值"换成读权威修订，step 语义不变。

- **Observation**：core 的写账本重放只还原**写状态**，不还原整条结果：`existingResult` 按当前记录重新组装结果，`runExternalId` 恒为 `undefined`（账本不存运行 id）。因此"同键重放返回原结果"在 core 层面只能断言到写状态与执行上下文 id。
  **Evidence**：`packages/core/src/start-work.ts` 的 `existingResult` 显式传 `runExternalId: undefined`；`tests/e2e/controller-roundtrip.test.js` 据此断言 `writeState` 与 `executionContextId` 相等。
  **Decision impact**：controller 额外加了一层实例内重放账本，使 `CommandResult` 整对象逐字相同；跨重启的持久账本仍属 Storage，受 Gate E1 约束，本批次不落库。

- **Observation**：一次 poll 跨过两个以上修订时，"上一快照 → 当前快照"的净增量其实可以构造，但 watch 仍然报 gap。原因是净增量的 `previousRevision` 只对停在上一快照的订阅者成立；没有逐实体版本时，任何中间游标都无法由它恢复。
  **Evidence**：`packages/controller/src/watch.ts` 的 `classify`；`tests/e2e/controller-roundtrip.test.js` 在保留窗口为 1 时落后两跳必须得到 gap。
  **Decision impact**：宁可让订阅者重拉一次基线，也不发一个只有部分游标能安全应用的增量；若后续引入逐实体版本或变更日志，可以把"净增量"作为合法续传。

- **Observation**：规划状态的显式写入在 wire 层没有"权威已保存"这个取值。provider 权威下 `applyPlanningStatus` 被拒绝（`failed`），宿主/人工权威下写入只在本地生效（`local_only`，`authoritative === false`）——因为 `authoritative` 命名的就是 provider 确认。
  **Evidence**：`packages/controller/src/commands.ts` 的 `CommandWriteState` 与 `applyPlanningStatus`；`tests/e2e/controller-roundtrip.test.js` 在两种权威下各断言一条。
  **Decision impact**：`confirmed` 只可能来自 provider ack / read-after-write，与 AGENTS.md §1.1 的硬约束同一语义，不给未来 UI 留下"乐观已保存"的入口。
## Decision Log

- **Decision**：进度断言独立成轨道，暂不进 `pnpm verify`，C5 才提升。
  **Rationale**：issue #42 要求断言因正确原因失败且禁止 skip；分支保护要求必需检查全绿。分开是同时满足两者的唯一办法，且提升时机与"变绿"重合。
  **Date/Author**：2026-09-20 / agent

- **Decision**：不做 SQLite 持久化，用 Storage 契约的内存替身验证重启恢复。
  **Rationale**：#27/#28 受 Gate E1 约束。本栈要证伪的是"控制事实是否被持久化到契约边界"，这条性质与具体表结构无关。
  **Date/Author**：2026-09-20 / agent

- **Decision**：组合根放测试层。
  **Rationale**：包边界矩阵不允许生产包同时 import `core` 与 `providers/*`；在测试层装配既满足边界，又让"MVP-0 用哪些替身"一目了然。
  **Date/Author**：2026-09-20 / agent

## Idempotence and Recovery

- 全部验证命令只读且可重复。
- 内存替身没有外部副作用，失败后重跑即可；不需要清理数据库或目录。
- 若某个断言在当前 head 上不可能通过（例如依赖尚未落地），必须把它写成"点名未实现节点"的失败，而不是 skip 或注释掉。
- 分支已推送后改写需先建恢复锚点并用精确 lease 强推。

## Interfaces and Dependencies

**依赖上游批次**

| 需要什么 | 来自 |
|---|---|
| 领域实体、标识、关系语义、错误码 | `packages/domain`（A1） |
| 五域 port、capability key、观察形状 | `packages/capabilities`（A2） |
| 离线替身（含 Storage 内存替身） | `packages/providers/fake`（A2/A3） |

**对外提供的接口**

```text
composeCore(deps) → CoreApi（查询 + 命令）
ProjectQueries / ProjectCommands / watchWorkspace
createClient(transport) → 客户端模型（React-free）
```

## Outcomes & Retrospective

完成后填写：实际跑通的链路、每个 head 上的失败断言数、与设计的偏差、留给 MVP-1 的未决项。

## Bottom Change Note

- 2026-09-20：首次创建。原因：切片是控制计划里最长的一条栈，需要把"先失败、再单调变绿"的过程写成可核对的记录。
